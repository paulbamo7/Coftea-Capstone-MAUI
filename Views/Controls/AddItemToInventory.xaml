<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Coftea_Capstone.Views.Controls.AddItemToInventory"
             xmlns:views="clr-namespace:Coftea_Capstone.Views.Pages"
             xmlns:converters="clr-namespace:Coftea_Capstone.ViewModel.Others"
             BackgroundColor="#80000000"
             IsVisible="{Binding IsAddItemToInventoryVisible}"
             InputTransparent="False">

    <ContentView.Resources>
        <ResourceDictionary>
            <converters:InvertBoolConverter x:Key="InvertBoolConverter"/>
        </ResourceDictionary>
    </ContentView.Resources>

    <!-- Add Item to Inventory Section -->
    <Grid ColumnDefinitions="200, *" RowDefinitions="*" InputTransparent="False">

        <!-- Background Gradient -->
        <VerticalStackLayout Grid.Column="1" Padding="30,60,0,0">
            <HorizontalStackLayout Spacing="20">
                <!-- Left Column -->
                <Frame MaximumHeightRequest="550" HeightRequest="600" WidthRequest="500" BackgroundColor="#F8E8D7" CornerRadius="20">
                    <VerticalStackLayout Spacing="15" Padding="20">
                        <Label Text="{Binding PageTitle}" FontAttributes="Bold" FontSize="28" HorizontalOptions="Start" TextColor="Black" Margin="0,0,0,20"/>
                        
                        <Label Text="STOCK NAME" FontSize="18" FontAttributes="Bold" TextColor="#8B4513"/>
                        <Entry Placeholder="Enter stock name" PlaceholderColor="Gray" Text="{Binding ItemName}" 
                               TextColor="Black" FontSize="16"/>

                        <Label Text="STOCK IMAGE" FontSize="18" FontAttributes="Bold" TextColor="#8B4513" Margin="0,20,0,0"/>
                        <Frame BorderColor="#D0D0D0" CornerRadius="15" HeightRequest="120" WidthRequest="450" BackgroundColor="White" 
                               HorizontalOptions="Center" HasShadow="True">
                            <Grid>
                                <Button BackgroundColor="Transparent" Command="{Binding PickImageCommand}" />
                                <Image Source="{Binding SelectedImageSource}" Aspect="AspectFit" />
                                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" 
                                                   IsVisible="{Binding SelectedImageSource, Converter={StaticResource InvertBoolConverter}}">
                                    <Image Source="uploadicon.png" WidthRequest="40" HeightRequest="40" HorizontalOptions="Center"/>
                                    <Label Text="Click to upload or drag and drop" HorizontalOptions="Center" 
                                           HorizontalTextAlignment="Center" TextColor="#666666" FontSize="14" Margin="0,10,0,0"/>
                                    <Label Text="PNG, JPG (max 3MB)" HorizontalOptions="Center" 
                                           HorizontalTextAlignment="Center" TextColor="#666666" FontSize="12"/>
                                </VerticalStackLayout>
                                <ImageButton Source="close.png" WidthRequest="20" HeightRequest="20" 
                                           HorizontalOptions="End" VerticalOptions="End" Margin="0,0,10,10"
                                           IsVisible="{Binding SelectedImageSource, Converter={StaticResource InvertBoolConverter}, ConverterParameter=Invert}"
                                           Command="{Binding ClearImageCommand}"/>
                            </Grid>
                        </Frame>

                        <Label Text="PRODUCT CATEGORY" FontSize="18" FontAttributes="Bold" TextColor="#8B4513" Margin="0,20,0,0"/>
                        <Picker Title="Select Category" 
                                ItemsSource="{Binding Categories}" 
                                SelectedItem="{Binding ItemCategory}"
                                FontSize="16"                  
                                TextColor="Black"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- Right Column -->
                <Frame MaximumHeightRequest="550" HeightRequest="600" WidthRequest="500" BackgroundColor="#F8E8D7" CornerRadius="20">
                    <ScrollView>
                        <VerticalStackLayout Spacing="7" Padding="20">
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Label Text="Measurement Details" FontAttributes="Bold" FontSize="28" VerticalOptions="Center" Grid.Column="0" TextColor="Black"/>
                                <ImageButton Source="close.png" WidthRequest="25" HeightRequest="25" Grid.Column="1" 
                                         VerticalOptions="Center" Command="{Binding CloseAddItemToInventoryCommand}"/>
                            </Grid>
                            <!-- Headers for UoM categories (Syrups, Powdered, Fruit series, Sinkers & etc) -->
                            <VerticalStackLayout HorizontalOptions="Center" IsVisible="{Binding IsUoMOnlyCategory}">
                                <HorizontalStackLayout Spacing="20">
                                    <Label Text="STOCK QUANTITY" FontSize="18" FontAttributes="Bold" TextColor="#8B4513"/>
                                    <Label Text="UNIT OF MEASUREMENT (UoM)" FontSize="18" FontAttributes="Bold" TextColor="#8B4513"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>

                            <!-- Headers for Others category -->
                            <VerticalStackLayout HorizontalOptions="Start" IsVisible="{Binding IsPiecesOnlyCategory}">
                                <HorizontalStackLayout Spacing="20">
                                    <Label Text="STOCK QUANTITY" FontSize="18" FontAttributes="Bold" TextColor="#8B4513"/>
                                    <Label Text="MEASUREMENT" FontSize="18" FontAttributes="Bold" TextColor="#8B4513"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>

                            <!-- UoM categories input: quantity entry + UoM picker -->
                            <VerticalStackLayout IsVisible="{Binding IsUoMOnlyCategory}">
                                <HorizontalStackLayout Spacing="20">
                                    <Entry Placeholder="Enter stock quantity" Text="{Binding UoMQuantity}" Keyboard="Numeric" 
                                       TextColor="Black" FontSize="16" WidthRequest="150"/>
                                    <Picker Title="Select UoM" ItemsSource="{Binding UoMOptions}" SelectedItem="{Binding SelectedUoM}"
                                        FontSize="16" WidthRequest="255" TextColor="Black"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>

                            <!-- Others category input: quantity + measurement -->
                            <VerticalStackLayout IsVisible="{Binding IsPiecesOnlyCategory}">
                                <HorizontalStackLayout Spacing="20">
                                    <Entry Placeholder="Enter quantity" Text="{Binding UoMQuantity}" Keyboard="Numeric" 
                                       TextColor="Black" FontSize="16" WidthRequest="150"/>
                                    <Picker Title="Select UoM" ItemsSource="{Binding UoMOptions}" SelectedItem="{Binding SelectedUoM}"
                                        FontSize="16" WidthRequest="255" TextColor="Black"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>

                            <!-- Minimum sections: show only one type based on category -->
                            <!-- Others category: show minimum quantity -->
                            <VerticalStackLayout IsVisible="{Binding IsPiecesOnlyCategory}">
                                <HorizontalStackLayout>
                                    <Label Text="MINIMUM QUANTITY LEVEL" FontSize="18" FontAttributes="Bold" TextColor="#8B4513" Margin="0,20,0,5"/>
                                    <Label Text="(Show alerts if met)" FontSize="14" TextColor="#666666" Margin="20,20,0,0"/>
                                </HorizontalStackLayout>
                                <HorizontalStackLayout Spacing="20">
                                    <Entry Placeholder="Enter minimum quantity" Text="{Binding MinimumQuantity}" Keyboard="Numeric" 
                                       TextColor="Black" FontSize="16" WidthRequest="200"/>
                                    <Picker Title="Select UoM" ItemsSource="{Binding UoMOptions}" SelectedItem="{Binding SelectedMinimumUoM}" FontSize="16"
                                        TextColor="Black" WidthRequest="200"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>

                            <!-- UoM categories: show minimum UoM -->
                            <VerticalStackLayout IsVisible="{Binding IsUoMOnlyCategory}">
                                <Label Text="MINIMUM UoM Level" FontSize="18" FontAttributes="Bold" TextColor="#8B4513" Margin="0,20,0,5"/>
                                <HorizontalStackLayout Spacing="10">
                                    <Entry Placeholder="Quantity" Text="{Binding MinimumUoMQuantity}" Keyboard="Numeric" 
                                       TextColor="Black" FontSize="16" WidthRequest="200"/>
                                    <Picker Title="Select UoM" ItemsSource="{Binding UoMOptions}" SelectedItem="{Binding SelectedMinimumUoM}" FontSize="16"
                                        TextColor="Black" WidthRequest="200"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>

                            <Label Text="DESCRIPTION (OPTIONAL)" FontSize="18" FontAttributes="Bold" TextColor="#8B4513" Margin="0,20,0,10"/>
                            <Frame BackgroundColor="White" 
                               CornerRadius="10" 
                               Padding="15"
                               HasShadow="False">
                                <Editor Placeholder="Enter item description..." 
                                HeightRequest="120" 
                                Text="{Binding ItemDescription}" 
                                BackgroundColor="Transparent"
                                TextColor="Black"
                                FontSize="16"/>
                            </Frame>

                            <Button Text="{Binding ButtonText}" WidthRequest="150" BackgroundColor="#FFD700" TextColor="Black" 
                                Command="{Binding AddItemCommand}" HorizontalOptions="End" Margin="0,20,0,0"
                                FontSize="16" FontAttributes="Bold" CornerRadius="25"/>
                        </VerticalStackLayout>
                    </ScrollView>
                </Frame>
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </Grid>
</ContentView>