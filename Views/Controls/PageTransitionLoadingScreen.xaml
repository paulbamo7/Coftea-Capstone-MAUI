<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Coftea_Capstone.Views.Controls.PageTransitionLoadingScreen"
             x:Name="LoadingScreen">

    <Grid BackgroundColor="#C1A892"
          VerticalOptions="FillAndExpand"
          HorizontalOptions="FillAndExpand">
        
        <!-- Background Gradient -->
        <Grid.Background>
            <LinearGradientBrush>
                <GradientStop Color="#C1A892" Offset="0.25" />
                <GradientStop Color="#5B4F45" Offset="0.85" />
            </LinearGradientBrush>
        </Grid.Background>

        <VerticalStackLayout VerticalOptions="Center"
                             HorizontalOptions="Center"
                             Spacing="30">

            <!-- Logo -->
            <Image Source="coftea_logo.png"
                   HeightRequest="200"
                   MaximumWidthRequest="300"
                   HorizontalOptions="Center" />

            <!-- Loading Animation -->
            <ActivityIndicator IsRunning="True"
                               Color="White"
                               WidthRequest="50"
                               HeightRequest="50" />

            <!-- Loading Text -->
            <Label Text="Loading..."
                   TextColor="White"
                   FontSize="18"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />

        </VerticalStackLayout>
    </Grid>
</ContentView>
