<?xml version="1.0" encoding="utf-8" ?>
<ContentView 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Coftea_Capstone.Views.Controls.ActivityLogPopup"
    xmlns:converters="clr-namespace:Coftea_Capstone.ViewModel.Others"
    x:Name="ActivityLogPopupView"
    BackgroundColor="#80000000"
    IsVisible="{Binding IsVisible}"
    InputTransparent="False">

    <ContentView.Resources>
        <ResourceDictionary>
            <converters:StringToBoolConverter x:Key="StringToBoolConverter"/>
            <converters:InvertBoolConverter x:Key="InvertBoolConverter"/>
        </ResourceDictionary>
    </ContentView.Resources>

    <Grid VerticalOptions="Fill" HorizontalOptions="Fill" InputTransparent="False">
        <!-- Background overlay that closes on tap -->
        <BoxView BackgroundColor="#80000000" InputTransparent="False" ZIndex="500">
            <BoxView.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding CloseCommand}" />
            </BoxView.GestureRecognizers>
        </BoxView>

        <!-- Date Filter Popup -->
        <Frame IsVisible="{Binding IsDateFilterVisible}"
               BackgroundColor="White"
               CornerRadius="15"
               Padding="20"
               WidthRequest="350"
               HeightRequest="450"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               HasShadow="True"
               InputTransparent="False"
               ZIndex="4000">
            <Grid RowDefinitions="Auto,*,Auto" RowSpacing="15">
                <!-- Header -->
                <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
                    <Label Text="📅 Filter by Date Range" 
                           FontSize="18" 
                           FontAttributes="Bold" 
                           TextColor="#5B4F45"
                           VerticalOptions="Center"/>
                    <Border Grid.Column="1"
                            WidthRequest="32"
                            HeightRequest="32"
                            BackgroundColor="#F5F5F5"
                            StrokeThickness="0"
                            StrokeShape="RoundRectangle 16"
                            HorizontalOptions="End"
                            VerticalOptions="Center">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding HideDateFilterCommand}"/>
                        </Border.GestureRecognizers>
                        <Label Text="✕"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="#5B4F45"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>
                    </Border>
                </Grid>
                
                <!-- Date inputs -->
                <VerticalStackLayout Grid.Row="1" Spacing="20">
                    <!-- Quick Filters -->
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Quick Filters" 
                               FontSize="14" 
                               FontAttributes="Bold" 
                               TextColor="#5B4F45"/>
                        <Grid ColumnDefinitions="*,*" ColumnSpacing="10" RowDefinitions="Auto,Auto" RowSpacing="10">
                            <Button Grid.Row="0" Grid.Column="0" 
                                    Text="Today" 
                                    Command="{Binding SetTodayCommand}" 
                                    BackgroundColor="#E8F5E8"
                                    TextColor="#2E7D32"
                                    CornerRadius="8"
                                    HeightRequest="40"/>
                            <Button Grid.Row="0" Grid.Column="1" 
                                    Text="Yesterday" 
                                    Command="{Binding SetYesterdayCommand}" 
                                    BackgroundColor="#E8F5E8"
                                    TextColor="#2E7D32"
                                    CornerRadius="8"
                                    HeightRequest="40"/>
                            <Button Grid.Row="1" Grid.Column="0" 
                                    Text="Last 7 Days" 
                                    Command="{Binding SetLast7DaysCommand}" 
                                    BackgroundColor="#E8F5E8"
                                    TextColor="#2E7D32"
                                    CornerRadius="8"
                                    HeightRequest="40"/>
                            <Button Grid.Row="1" Grid.Column="1" 
                                    Text="Last 30 Days" 
                                    Command="{Binding SetLast30DaysCommand}" 
                                    BackgroundColor="#E8F5E8"
                                    TextColor="#2E7D32"
                                    CornerRadius="8"
                                    HeightRequest="40"/>
                        </Grid>
                    </VerticalStackLayout>
                    
                    <!-- Custom Date Range -->
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Custom Range" 
                               FontSize="14" 
                               FontAttributes="Bold" 
                               TextColor="#5B4F45"/>
                        <VerticalStackLayout Spacing="8">
                            <Label Text="Start Date" 
                                   FontSize="12" 
                                   TextColor="#666666"/>
                            <DatePicker x:Name="ActivityStartDatePicker"
                                       Date="{Binding FilterStartDate, Mode=TwoWay}"
                                       MinimumDate="2020-01-01"
                                       MaximumDate="{Binding FilterEndDate}"
                                       TextColor="#5B4F45"
                                       BackgroundColor="#F5F5F5"
                                       FontSize="14"
                                       DateSelected="OnActivityStartDateSelected"/>
                        </VerticalStackLayout>
                        <VerticalStackLayout Spacing="8">
                            <Label Text="End Date" 
                                   FontSize="12" 
                                   TextColor="#666666"/>
                            <DatePicker x:Name="ActivityEndDatePicker"
                                       Date="{Binding FilterEndDate, Mode=TwoWay}"
                                       MinimumDate="{Binding FilterStartDate}"
                                       MaximumDate="{Binding Today}"
                                       TextColor="#5B4F45"
                                       BackgroundColor="#F5F5F5"
                                       FontSize="14"/>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </VerticalStackLayout>
                
                <!-- Buttons -->
                <Grid Grid.Row="2" ColumnDefinitions="*,*" ColumnSpacing="10">
                    <Button Grid.Column="0" 
                            Text="Clear Filter" 
                            Command="{Binding ClearDateFilterCommand}"
                            BackgroundColor="#E0E0E0" 
                            TextColor="#666666" 
                            CornerRadius="8"
                            HeightRequest="45"/>
                    <Button Grid.Column="1" 
                            Text="Apply Filter" 
                            Command="{Binding ApplyDateFilterCommand}"
                            BackgroundColor="#2E7D32" 
                            TextColor="White" 
                            CornerRadius="8"
                            HeightRequest="45"/>
                </Grid>
            </Grid>
        </Frame>

        <!-- Main popup content -->
        <Frame BackgroundColor="White" 
               CornerRadius="20" 
               Padding="0" 
               HasShadow="True"
               Margin="20"
               VerticalOptions="Center"
               HorizontalOptions="Center"
               WidthRequest="920"
               HeightRequest="700"
               InputTransparent="False"
               ZIndex="1000">

            <Grid RowDefinitions="Auto, *, Auto">
                <!-- Header -->
                <Grid Grid.Row="0" BackgroundColor="#7E6D5F" Padding="20">
                    <Grid ColumnDefinitions="*,Auto,Auto">
                        <Label Grid.Column="0" 
                               Text="Inventory Activity Log" 
                               FontSize="24" FontFamily="MontserratBlack" 
                               FontAttributes="Bold" 
                               TextColor="White" 
                               VerticalOptions="Center"/>
                        <Button Grid.Column="1"
                                Text="📄 Export PDF"
                                BackgroundColor="#F3D5B5"
                                TextColor="#5B4F45"
                                BorderColor="#8B7355"
                                BorderWidth="2"
                                CornerRadius="18"
                                FontSize="13"
                                HeightRequest="40"
                                Padding="16,0"
                                HorizontalOptions="End"
                                VerticalOptions="Center"
                                Command="{Binding ShowPrintDialogCommand}"/>
                        <Button Grid.Column="2" 
                                    Text="×" 
                                BackgroundColor="Transparent" 
                                TextColor="White" 
                                FontSize="32" 
                                FontAttributes="Bold"
                                WidthRequest="40" 
                                HeightRequest="40"
                                Padding="0"
                                Command="{Binding CloseCommand}"/>
                    </Grid>
                </Grid>

                <!-- Content Area -->
                <Grid Grid.Row="1" Padding="20">
                    <!-- Loading Indicator -->
                    <VerticalStackLayout IsVisible="{Binding IsLoading}" 
                                        HorizontalOptions="Center" 
                                        VerticalOptions="Center">
                        <ActivityIndicator IsRunning="{Binding IsLoading}" 
                                         IsVisible="{Binding IsLoading}" 
                                         Color="#7E6D5F"/>
                        <Label Text="Loading activity log..." 
                               TextColor="Black" 
                               FontSize="14" FontFamily="MontserratBlack" 
                               HorizontalOptions="Center" 
                               Margin="0,10,0,0"/>
                    </VerticalStackLayout>

                    <!-- Filter Controls -->
                    <Grid IsVisible="{Binding IsLoading, Converter={StaticResource InvertBoolConverter}}" 
                          RowDefinitions="Auto, Auto, Auto, *" 
                          RowSpacing="10">
                        
                        <!-- Filter Buttons -->
                        <StackLayout Grid.Row="0" Orientation="Horizontal" Spacing="10" HorizontalOptions="Start">
                            <Button Text="All" 
                                    Command="{Binding FilterByActionCommand}" 
                                    CommandParameter="All"
                                    BackgroundColor="#BDAEA2"
                                    TextColor="#5B4F45"
                                    BorderColor="#8B7355"
                                    BorderWidth="2"
                                    FontAttributes="Bold"
                                    FontSize="14"
                                    HeightRequest="45"
                                    WidthRequest="90"
                                    CornerRadius="20">
                                <Button.Triggers>
                                    <DataTrigger TargetType="Button" Binding="{Binding SelectedFilter}" Value="All">
                                        <Setter Property="BackgroundColor" Value="#D4A574" />
                                        <Setter Property="TextColor" Value="White" />
                                        <Setter Property="BorderWidth" Value="0" />
                                    </DataTrigger>
                                </Button.Triggers>
                            </Button>
                            <Button Text="Deductions" 
                                    Command="{Binding FilterByActionCommand}" 
                                    CommandParameter="DEDUCTED"
                                    BackgroundColor="#BDAEA2"
                                    TextColor="#5B4F45"
                                    BorderColor="#8B7355"
                                    BorderWidth="2"
                                    FontAttributes="Bold"
                                    FontSize="14"
                                    HeightRequest="45"
                                    WidthRequest="120"
                                    CornerRadius="20">
                                <Button.Triggers>
                                    <DataTrigger TargetType="Button" Binding="{Binding SelectedFilter}" Value="DEDUCTED">
                                        <Setter Property="BackgroundColor" Value="#D4A574" />
                                        <Setter Property="TextColor" Value="White" />
                                        <Setter Property="BorderWidth" Value="0" />
                                    </DataTrigger>
                                </Button.Triggers>
                            </Button>
                            <Button Text="Additions" 
                                    Command="{Binding FilterByActionCommand}" 
                                    CommandParameter="ADDED"
                                    BackgroundColor="#BDAEA2"
                                    TextColor="#5B4F45"
                                    BorderColor="#8B7355"
                                    BorderWidth="2"
                                    FontAttributes="Bold"
                                    FontSize="14"
                                    HeightRequest="45"
                                    WidthRequest="120"
                                    CornerRadius="20">
                                <Button.Triggers>
                                    <DataTrigger TargetType="Button" Binding="{Binding SelectedFilter}" Value="ADDED">
                                        <Setter Property="BackgroundColor" Value="#D4A574" />
                                        <Setter Property="TextColor" Value="White" />
                                        <Setter Property="BorderWidth" Value="0" />
                                    </DataTrigger>
                                </Button.Triggers>
                            </Button>
                            <Button Text="Retracted" 
                                    Command="{Binding FilterByActionCommand}" 
                                    CommandParameter="RETRACTED"
                                    BackgroundColor="#BDAEA2"
                                    TextColor="#5B4F45"
                                    BorderColor="#8B7355"
                                    BorderWidth="2"
                                    FontAttributes="Bold"
                                    FontSize="14"
                                    HeightRequest="45"
                                    WidthRequest="120"
                                    CornerRadius="20">
                                <Button.Triggers>
                                    <DataTrigger TargetType="Button" Binding="{Binding SelectedFilter}" Value="RETRACTED">
                                        <Setter Property="BackgroundColor" Value="#D4A574" />
                                        <Setter Property="TextColor" Value="White" />
                                        <Setter Property="BorderWidth" Value="0" />
                                    </DataTrigger>
                                </Button.Triggers>
                            </Button>
                        </StackLayout>

                        <!-- Search and Date Filter -->
                        <Grid Grid.Row="1" ColumnDefinitions="*, Auto, Auto" ColumnSpacing="10">
                            <Border Grid.Column="0" 
                                    Stroke="#8B7355" 
                                    StrokeShape="RoundRectangle 15" 
                                    StrokeThickness="2" 
                                    HeightRequest="45" 
                                    BackgroundColor="White">
                                <Entry Placeholder="Search by item name..." 
                                       Text="{Binding SearchText}"
                                       BackgroundColor="Transparent"
                                       TextColor="Black"
                                       PlaceholderColor="#8B7355"
                                       Margin="15,0"/>
                            </Border>
                            <Button Grid.Column="1" 
                                    Text="📅 Filter Date" 
                                    BackgroundColor="#BDAEA2" 
                                    TextColor="#5B4F45" 
                                    BorderColor="#8B7355"
                                    BorderWidth="2"
                                    WidthRequest="130" 
                                    HeightRequest="45"
                                    CornerRadius="20"
                                    FontSize="13"
                                    Command="{Binding ShowDateFilterCommand}">
                                <Button.Triggers>
                                    <DataTrigger TargetType="Button" Binding="{Binding HasDateFilter}" Value="True">
                                        <Setter Property="BackgroundColor" Value="#D4A574" />
                                        <Setter Property="TextColor" Value="White" />
                                        <Setter Property="BorderWidth" Value="0" />
                                    </DataTrigger>
                                </Button.Triggers>
                            </Button>
                            <Button Grid.Column="2" 
                                    Text="🔄" 
                                    BackgroundColor="#BDAEA2" 
                                    TextColor="#5B4F45" 
                                    BorderColor="#8B7355"
                                    BorderWidth="2"
                                    WidthRequest="55" 
                                    HeightRequest="45"
                                    CornerRadius="20"
                                    Command="{Binding RefreshCommand}"/>
                        </Grid>
                        
                        <!-- Date Filter Indicator -->
                        <Grid Grid.Row="2" IsVisible="{Binding HasDateFilter}" ColumnDefinitions="*" Margin="0,5,0,0">
                            <Border BackgroundColor="#E8F5E9" 
                                    Stroke="#4CAF50" 
                                    StrokeShape="RoundRectangle 8" 
                                    StrokeThickness="1" 
                                    Padding="10,5">
                                <HorizontalStackLayout Spacing="8" HorizontalOptions="Start">
                                    <Label Text="📅" FontSize="14" VerticalOptions="Center"/>
                                    <Label Text="{Binding DateFilterText}" 
                                           FontSize="12" 
                                           TextColor="#2E7D32" 
                                           FontAttributes="Bold"
                                           VerticalOptions="Center"/>
                                    <Button Text="✕" 
                                            BackgroundColor="Transparent" 
                                            TextColor="#2E7D32" 
                                            FontSize="16"
                                            Padding="5,0"
                                            Command="{Binding ClearDateFilterCommand}"/>
                                </HorizontalStackLayout>
                            </Border>
                        </Grid>

                        <!-- Activity Log Table -->
                        <ScrollView Grid.Row="3" Orientation="Vertical" VerticalScrollBarVisibility="Default">
                            <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Always">
                                <VerticalStackLayout Spacing="0">
                                    <!-- Table Header -->
                                    <Grid BackgroundColor="#7E6D5F" Padding="10,8" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>    <!-- # -->
                                        <ColumnDefinition Width="1.1*"/>   <!-- Date & Time -->
                                        <ColumnDefinition Width="1*"/>     <!-- User -->
                                        <ColumnDefinition Width="0.9*"/>   <!-- Action -->
                                        <ColumnDefinition Width="1.2*"/>   <!-- Item -->
                                        <ColumnDefinition Width="0.8*"/>   <!-- Qty Change -->
                                        <ColumnDefinition Width="0.6*"/>   <!-- UoM -->
                                        <ColumnDefinition Width="0.8*"/>   <!-- Prev Qty -->
                                        <ColumnDefinition Width="0.8*"/>   <!-- New Qty -->
                                        <ColumnDefinition Width="1.1*"/>   <!-- Used for Product -->
                                        <ColumnDefinition Width="1.3*"/>   <!-- Remarks -->
                                    </Grid.ColumnDefinitions>
                                    
                                    <Label Grid.Column="0" Text="#" FontSize="11" FontAttributes="Bold" TextColor="White" HorizontalOptions="Start"/>
                                    <Label Grid.Column="1" Text="Date &amp; Time" FontSize="11" FontAttributes="Bold" TextColor="White"/>
                                    <Label Grid.Column="2" Text="User" FontSize="11" FontAttributes="Bold" TextColor="White"/>
                                    <Label Grid.Column="3" Text="Action" FontSize="11" FontAttributes="Bold" TextColor="White"/>
                                    <Label Grid.Column="4" Text="Item" FontSize="11" FontAttributes="Bold" TextColor="White"/>
                                    <Label Grid.Column="5" Text="Qty Change" FontSize="11" FontAttributes="Bold" TextColor="White" HorizontalOptions="End"/>
                                    <Label Grid.Column="6" Text="UoM" FontSize="11" FontAttributes="Bold" TextColor="White"/>
                                    <Label Grid.Column="7" Text="Prev Qty" FontSize="11" FontAttributes="Bold" TextColor="White" HorizontalOptions="End"/>
                                    <Label Grid.Column="8" Text="New Qty" FontSize="11" FontAttributes="Bold" TextColor="White" HorizontalOptions="End"/>
                                    <Label Grid.Column="9" Text="Used for Product" FontSize="11" FontAttributes="Bold" TextColor="White"/>
                                    <Label Grid.Column="10" Text="Remarks" FontSize="11" FontAttributes="Bold" TextColor="White"/>
                                </Grid>

                                <!-- Table Rows -->
                                <CollectionView ItemsSource="{Binding PagedActivityLog}">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Padding="10,8" BackgroundColor="{Binding RowBackgroundColor}" ColumnSpacing="12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>    <!-- # -->
                                                    <ColumnDefinition Width="1.1*"/>   <!-- Date & Time -->
                                                    <ColumnDefinition Width="1*"/>     <!-- User -->
                                                    <ColumnDefinition Width="0.9*"/>   <!-- Action -->
                                                    <ColumnDefinition Width="1.2*"/>   <!-- Item -->
                                                    <ColumnDefinition Width="0.8*"/>   <!-- Qty Change -->
                                                    <ColumnDefinition Width="0.6*"/>   <!-- UoM -->
                                                    <ColumnDefinition Width="0.8*"/>   <!-- Prev Qty -->
                                                    <ColumnDefinition Width="0.8*"/>   <!-- New Qty -->
                                                    <ColumnDefinition Width="1.1*"/>   <!-- Used for Product -->
                                                    <ColumnDefinition Width="1.3*"/>   <!-- Remarks -->
                                                </Grid.ColumnDefinitions>
                                                
                                                <Label Grid.Column="0" Text="{Binding RowNumber}" FontSize="10" TextColor="#333" HorizontalOptions="Start" VerticalOptions="Center"/>
                                                <Label Grid.Column="1" Text="{Binding FormattedTimestampShort}" FontSize="10" TextColor="#333" VerticalOptions="Center"/>
                                                <Label Grid.Column="2" Text="{Binding UserFullName}" FontSize="10" TextColor="#333" LineBreakMode="TailTruncation" VerticalOptions="Center"/>
                                                <Label Grid.Column="3" Text="{Binding ActionText}" FontSize="10" TextColor="{Binding ColorCode}" FontAttributes="Bold" VerticalOptions="Center"/>
                                                <Label Grid.Column="4" Text="{Binding ItemName}" FontSize="10" TextColor="#333" LineBreakMode="TailTruncation" VerticalOptions="Center"/>
                                                <Label Grid.Column="5" Text="{Binding QuantityChangeText}" FontSize="10" TextColor="{Binding ColorCode}" FontAttributes="Bold" HorizontalOptions="End" VerticalOptions="Center"/>
                                                <Label Grid.Column="6" Text="{Binding UnitOfMeasurement}" FontSize="10" TextColor="#666" VerticalOptions="Center"/>
                                                <Label Grid.Column="7" Text="{Binding PreviousQuantityText}" FontSize="10" TextColor="#666" HorizontalOptions="End" VerticalOptions="Center"/>
                                                <Label Grid.Column="8" Text="{Binding NewQuantityText}" FontSize="10" TextColor="#333" FontAttributes="Bold" HorizontalOptions="End" VerticalOptions="Center"/>
                                                <Label Grid.Column="9" Text="{Binding UsedForProductText}" FontSize="10" TextColor="#4A90E2" FontAttributes="Bold" LineBreakMode="WordWrap" VerticalOptions="Center"/>
                                                <Label Grid.Column="10" Text="{Binding RemarksText}" FontSize="10" TextColor="#666" LineBreakMode="WordWrap" VerticalOptions="Center"/>
                                                
                                                <!-- Separator Line -->
                                                <BoxView Grid.ColumnSpan="11" HeightRequest="1" BackgroundColor="#E0E0E0" VerticalOptions="End"/>
                                            </Grid>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>
                                    <!-- Pagination Controls -->
                                    <Grid Padding="10" BackgroundColor="#FFF" ColumnDefinitions="Auto,*,Auto" Margin="0,5,0,0" MinimumWidthRequest="1100">
                                        <Button Grid.Column="0" Text="Prev" BackgroundColor="#BDAEA2" TextColor="#5B4F45" CornerRadius="8"
                                                IsEnabled="{Binding CanGoToPreviousPage}"
                                                Command="{Binding PrevPageCommand}"/>
                                        <Label Grid.Column="1" Text="{Binding CurrentPage, StringFormat='Page {0}'}" HorizontalOptions="Center" VerticalOptions="Center"/>
                                        <Button Grid.Column="2" Text="Next" BackgroundColor="#BDAEA2" TextColor="#5B4F45" CornerRadius="8"
                                                IsEnabled="{Binding CanGoToNextPage}"
                                                Command="{Binding NextPageCommand}"/>
                                    </Grid>
                                </VerticalStackLayout>
                            </ScrollView>
                        </ScrollView>
                    </Grid>
                </Grid>

                <!-- Footer -->
                <Grid Grid.Row="2" BackgroundColor="#F5F5F5" Padding="20">
                    <Label Text="{Binding StatusMessage}" 
                           TextColor="Gray" 
                           FontSize="12" FontFamily="MontserratBlack" 
                           HorizontalOptions="Center"/>
                </Grid>
            </Grid>
        </Frame>

        <!-- Print Confirmation / Status Dialog -->
        <Grid IsVisible="{Binding IsPrintDialogVisible}"
              BackgroundColor="#80000000"
              InputTransparent="False"
              ZIndex="4500"
              Padding="20">
            <Frame BackgroundColor="White"
                   CornerRadius="18"
                   Padding="24"
                   WidthRequest="380"
                   HasShadow="True"
                   HorizontalOptions="Center"
                   VerticalOptions="Center">
                <VerticalStackLayout Spacing="18">
                    <Label Text="{Binding PrintDialogTitle}"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#5B4F45"
                           HorizontalOptions="Center"/>

                    <ScrollView HeightRequest="160">
                        <Label Text="{Binding PrintDialogMessage}"
                               FontSize="13"
                               TextColor="#574D45"
                               HorizontalOptions="Center"
                               HorizontalTextAlignment="Center"/>
                    </ScrollView>

                    <ActivityIndicator IsVisible="{Binding IsPrintProcessing}"
                                       IsRunning="{Binding IsPrintProcessing}"
                                       Color="#5B4F45"
                                       HorizontalOptions="Center"/>

                    <HorizontalStackLayout Spacing="12" HorizontalOptions="Fill">
                        <Button Text="{Binding PrintDialogRejectText}"
                                BackgroundColor="#E0E0E0"
                                TextColor="#5B4F45"
                                CornerRadius="12"
                                HeightRequest="42"
                                Command="{Binding RejectPrintDialogCommand}"
                                HorizontalOptions="FillAndExpand"
                                IsVisible="{Binding PrintDialogHasReject}"/>
                        <Button Text="{Binding PrintDialogAcceptText}"
                                BackgroundColor="#5B4F45"
                                TextColor="White"
                                CornerRadius="12"
                                HeightRequest="42"
                                Command="{Binding AcceptPrintDialogAsyncCommand}"
                                HorizontalOptions="FillAndExpand"
                                IsEnabled="{Binding IsPrintProcessing, Converter={StaticResource InvertBoolConverter}}"/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Frame>
        </Grid>

        <!-- Close Confirmation Dialog -->
        <Grid IsVisible="{Binding IsCloseConfirmationVisible}"
              BackgroundColor="#80000000"
              InputTransparent="False"
              ZIndex="5000"
              Padding="20">
            <Frame BackgroundColor="White"
                   CornerRadius="18"
                   Padding="24"
                   WidthRequest="360"
                   HasShadow="True"
                   HorizontalOptions="Center"
                   VerticalOptions="Center">
                <VerticalStackLayout Spacing="16">
                    <Label Text="Close Activity Log?"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#5B4F45"
                           HorizontalOptions="Center"/>
                    <Label Text="Any selections and filters will stay in place the next time you open the log."
                           FontSize="13"
                           TextColor="#574D45"
                           HorizontalTextAlignment="Center"/>
                    <HorizontalStackLayout Spacing="12" HorizontalOptions="Fill">
                        <Button Text="Stay"
                                BackgroundColor="#E0E0E0"
                                TextColor="#5B4F45"
                                CornerRadius="12"
                                HeightRequest="42"
                                Command="{Binding CancelCloseCommand}"
                                HorizontalOptions="FillAndExpand"/>
                        <Button Text="Close"
                                BackgroundColor="#5B4F45"
                                TextColor="White"
                                CornerRadius="12"
                                HeightRequest="42"
                                Command="{Binding ConfirmCloseCommand}"
                                HorizontalOptions="FillAndExpand"/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Frame>
        </Grid>
    </Grid>
</ContentView>
