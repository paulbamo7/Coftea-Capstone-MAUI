<?xml version="1.0" encoding="utf-8"?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Coftea_Capstone.Views.Controls.AddItemToPOS"
    xmlns:converters="clr-namespace:Coftea_Capstone.ViewModel.Others"
    BackgroundColor="#80000000"
    IsVisible="{Binding IsAddItemToPOSVisible}"
    InputTransparent="False">

    <ContentView.Resources>
        <ResourceDictionary>
            <converters:InvertBoolConverter x:Key="InvertBoolConverter" />
            <converters:NullToBoolConverter x:Key="NullToBoolConverter" />
            <converters:NullToInvertedBoolConverter x:Key="NullToInvertedBoolConverter" />
            <converters:StringToBoolConverter x:Key="StringToBoolConverter" />
            <converters:ValidationBorderColorConverter x:Key="ValidationBorderColorConverter" />
        </ResourceDictionary>
    </ContentView.Resources>

    <AbsoluteLayout HorizontalOptions="Fill" VerticalOptions="Fill">
        <!-- Click-blocking overlay -->
        <BoxView BackgroundColor="#80000000" 
                 HorizontalOptions="Fill" 
                 VerticalOptions="Fill"
                 AbsoluteLayout.LayoutBounds="0,0,1,1"
                 AbsoluteLayout.LayoutFlags="All"
                 InputTransparent="{Binding IsAnyDropdownVisible}">
            <BoxView.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding CloseAddItemToPOSCommand}"/>
            </BoxView.GestureRecognizers>
        </BoxView>
        
        <!-- Popup Content -->
        <Frame HorizontalOptions="Center" 
               VerticalOptions="Center"
               BackgroundColor="#F8E8D7"
               CornerRadius="20"
               HasShadow="True"
               Padding="0"
               WidthRequest="1050"
               HeightRequest="700"
               AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1"
               AbsoluteLayout.LayoutFlags="PositionProportional"
               InputTransparent="False">

            <Grid ColumnDefinitions="500,2,*" RowDefinitions="*" BackgroundColor="#F8E8D7">

                <!-- LEFT PANEL -->
                <VerticalStackLayout Padding="27" BackgroundColor="#F8E8D7">
                    <Label Text="Add Item to POS"
                           FontAttributes="Bold"
                           FontSize="28" FontFamily="Montserrat-Black"
                           Margin="0,0,0,20"
                           TextColor="#5B4F45" />

                    <!-- PRODUCT NAME -->
                    <Label Text="PRODUCT NAME" FontAttributes="Bold" FontSize="18" FontFamily="OpenSansSemibold" TextColor="#5B4F45" Padding="0,20,0,0"/>
                    <Border Stroke="{Binding ProductNameValidationMessage, Converter={StaticResource ValidationBorderColorConverter}}" 
                            StrokeShape="RoundRectangle 10" 
                            StrokeThickness="2" 
                            HeightRequest="45"
                            Margin="0,0,0,5">
                        <Entry Placeholder="Enter product name"
                               Text="{Binding ProductName}"
                               FontSize="17"
                               TextColor="#5B4F45"
                               PlaceholderColor="#8B7355"
                               BackgroundColor="Transparent"
                               Margin="12,0"/>
                    </Border>
                    <Label Text="{Binding ProductNameValidationMessage}"
                           TextColor="#D9534F"
                           FontSize="12"
                           Margin="5,-5,0,5"
                           IsVisible="{Binding ProductNameValidationMessage, Converter={StaticResource StringToBoolConverter}}"/>

                    <!-- CATEGORY -->
                    <Label Text="CATEGORY" FontAttributes="Bold" FontSize="18" FontFamily="OpenSansSemibold" TextColor="#5B4F45" Margin="0,20,0,5" />
                    <AbsoluteLayout Margin="0,0,0,5">
                        <Button x:Name="CategoryButton"
                                Text="{Binding CategoryDisplayText}"
                                Command="{Binding ShowCategoryPickerCommand}"
                                BackgroundColor="White"
                                TextColor="#5B4F45"
                                BorderColor="#8B7355"
                                BorderWidth="1"
                                CornerRadius="10"
                                HeightRequest="45"
                                Padding="12,8"
                                AbsoluteLayout.LayoutBounds="0,0,200,45"
                                AbsoluteLayout.LayoutFlags="None"/>
                        
                        <!-- Category Dropdown -->
                        <Frame IsVisible="{Binding IsCategoryDropdownVisible}"
                               BackgroundColor="White"
                               CornerRadius="8"
                               HasShadow="True"
                               Padding="0"
                               WidthRequest="200"
                               MaximumHeightRequest="240"
                               AbsoluteLayout.LayoutBounds="210,0,200,240"
                               AbsoluteLayout.LayoutFlags="None"
                               ZIndex="10000"
                               InputTransparent="False">
                            <Grid RowDefinitions="Auto,*">
                                <Grid Grid.Row="0" BackgroundColor="#7E6D5F" Padding="10,6" ColumnDefinitions="*,Auto">
                                    <Label Text="Select Category" 
                                           FontSize="13" FontFamily="MontserratBlack" 
                                           FontAttributes="Bold" 
                                           TextColor="White" 
                                           Grid.Column="0"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center"/>
                                    <Button Text="✕" 
                                            FontSize="14" 
                                            TextColor="White" 
                                            BackgroundColor="Transparent" 
                                            Grid.Column="1"
                                            WidthRequest="24"
                                            HeightRequest="24"
                                            Padding="0"
                                            HorizontalOptions="End"
                                            VerticalOptions="Center"
                                            Command="{Binding CloseCategoryDropdownCommand}"/>
                                </Grid>
                                <ScrollView Grid.Row="1" 
                                          VerticalOptions="FillAndExpand"
                                          MaximumHeightRequest="200">
                                    <CollectionView ItemsSource="{Binding Categories}"
                                                  SelectionMode="None">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate>
                                            <Frame BackgroundColor="#F8E8D7" 
                                                   CornerRadius="0" 
                                                   Padding="10,8" 
                                                   HasShadow="False"
                                                   Margin="0,0,0,1"
                                                   MinimumHeightRequest="36">
                                                <Frame.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Source={x:Reference CategoryButton}, Path=BindingContext.SelectCategoryCommand}" 
                                                                          CommandParameter="{Binding .}"/>
                                                </Frame.GestureRecognizers>
                                                <Label Text="{Binding .}" 
                                                       FontSize="13" 
                                                       FontFamily="OpenSansSemibold"
                                                       TextColor="#5B4F45"
                                                       VerticalOptions="Center"
                                                       VerticalTextAlignment="Center"/>
                                            </Frame>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                    </CollectionView>
                                </ScrollView>
                            </Grid>
                        </Frame>
                    </AbsoluteLayout>
                    <Label Text="{Binding CategoryValidationMessage}"
                           TextColor="#D9534F"
                           FontSize="12"
                           Margin="5,-5,0,5"
                           IsVisible="{Binding CategoryValidationMessage, Converter={StaticResource StringToBoolConverter}}"/>

                    <!-- SUBCATEGORY for Fruit/Soda -->
                    <Label Text="SUBCATEGORY" FontAttributes="Bold" FontSize="18" FontFamily="OpenSansSemibold" TextColor="#5B4F45" Margin="0,20,0,5"
                           IsVisible="{Binding IsFruitSodaSubcategoryVisible}" />
                    <AbsoluteLayout IsVisible="{Binding IsFruitSodaSubcategoryVisible}">
                        <Button x:Name="FruitSodaSubcategoryButton"
                                Text="{Binding SubcategoryDisplayText}"
                                Command="{Binding ShowSubcategoryPickerCommand}"
                                BackgroundColor="White"
                                TextColor="#5B4F45"
                                BorderColor="#8B7355"
                                BorderWidth="1"
                                CornerRadius="10"
                                HeightRequest="45"
                                HorizontalOptions="Start"
                                Padding="12,8"
                                AbsoluteLayout.LayoutBounds="0,0,200,45"
                                AbsoluteLayout.LayoutFlags="None"/>
                        
                        <!-- Subcategory Dropdown -->
                        <Frame IsVisible="{Binding IsSubcategoryDropdownVisible}"
                               BackgroundColor="White"
                               CornerRadius="8"
                               HasShadow="True"
                               Padding="0"
                               WidthRequest="200"
                               MaximumHeightRequest="160"
                               AbsoluteLayout.LayoutBounds="210,0,200,160"
                               AbsoluteLayout.LayoutFlags="None"
                               ZIndex="10000"
                               InputTransparent="False">
                            <Grid RowDefinitions="Auto,*">
                                <Grid Grid.Row="0" BackgroundColor="#7E6D5F" Padding="10,6" ColumnDefinitions="*,Auto">
                                    <Label Text="Select Subcategory" 
                                           FontSize="13" FontFamily="MontserratBlack" 
                                           FontAttributes="Bold" 
                                           TextColor="White" 
                                           Grid.Column="0"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center"/>
                                    <Button Text="✕" 
                                            FontSize="14" 
                                            TextColor="White" 
                                            BackgroundColor="Transparent" 
                                            Grid.Column="1"
                                            WidthRequest="24"
                                            HeightRequest="24"
                                            Padding="0"
                                            HorizontalOptions="End"
                                            VerticalOptions="Center"
                                            Command="{Binding CloseSubcategoryDropdownCommand}"/>
                                </Grid>
                                <ScrollView Grid.Row="1"
                                          MaximumHeightRequest="120">
                                    <CollectionView ItemsSource="{Binding FruitSodaSubcategories}"
                                                  SelectionMode="None">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate>
                                            <Frame BackgroundColor="#F8E8D7" 
                                                   CornerRadius="0" 
                                                   Padding="10,8" 
                                                   HasShadow="False"
                                                   Margin="0,0,0,1"
                                                   MinimumHeightRequest="36">
                                                <Frame.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Source={x:Reference FruitSodaSubcategoryButton}, Path=BindingContext.SelectSubcategoryCommand}" 
                                                                          CommandParameter="{Binding .}"/>
                                                </Frame.GestureRecognizers>
                                                <Label Text="{Binding .}" 
                                                       FontSize="13" 
                                                       FontFamily="OpenSansSemibold"
                                                       TextColor="#5B4F45"
                                                       VerticalOptions="Center"
                                                       VerticalTextAlignment="Center"/>
                                            </Frame>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                    </CollectionView>
                                </ScrollView>
                            </Grid>
                        </Frame>
                    </AbsoluteLayout>
                    <Label Text="{Binding SubcategoryValidationMessage}"
                           TextColor="#D9534F"
                           FontSize="12"
                           Margin="5,-5,0,5"
                           IsVisible="{Binding IsFruitSodaSubcategoryVisible}">
                        <Label.Triggers>
                            <DataTrigger TargetType="Label" Binding="{Binding SubcategoryValidationMessage, Converter={StaticResource StringToBoolConverter}}" Value="False">
                                <Setter Property="IsVisible" Value="False"/>
                            </DataTrigger>
                        </Label.Triggers>
                    </Label>

                    <!-- SUBCATEGORY for Coffee -->
                    <Label Text="SUBCATEGORY" FontAttributes="Bold" FontSize="18" FontFamily="OpenSansSemibold" TextColor="#5B4F45" Margin="0,20,0,5"
                           IsVisible="{Binding IsCoffeeSubcategoryVisible}" />
                    <AbsoluteLayout IsVisible="{Binding IsCoffeeSubcategoryVisible}">
                        <Button x:Name="CoffeeSubcategoryButton"
                                Text="{Binding SubcategoryDisplayText}"
                                Command="{Binding ShowSubcategoryPickerCommand}"
                                BackgroundColor="White"
                                TextColor="#5B4F45"
                                BorderColor="#8B7355"
                                BorderWidth="1"
                                CornerRadius="10"
                                HeightRequest="45"
                                HorizontalOptions="Start"
                                Padding="12,8"
                                AbsoluteLayout.LayoutBounds="0,0,200,45"
                                AbsoluteLayout.LayoutFlags="None"/>
                        
                        <!-- Subcategory Dropdown -->
                        <Frame IsVisible="{Binding IsSubcategoryDropdownVisible}"
                               BackgroundColor="White"
                               CornerRadius="8"
                               HasShadow="True"
                               Padding="0"
                               WidthRequest="200"
                               MaximumHeightRequest="160"
                               AbsoluteLayout.LayoutBounds="210,0,200,160"
                               AbsoluteLayout.LayoutFlags="None"
                               ZIndex="10000"
                               InputTransparent="False">
                            <Grid RowDefinitions="Auto,*">
                                <Grid Grid.Row="0" BackgroundColor="#7E6D5F" Padding="10,6" ColumnDefinitions="*,Auto">
                                    <Label Text="Select Subcategory" 
                                           FontSize="13" FontFamily="MontserratBlack" 
                                           FontAttributes="Bold" 
                                           TextColor="White" 
                                           Grid.Column="0"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center"/>
                                    <Button Text="✕" 
                                            FontSize="14" 
                                            TextColor="White" 
                                            BackgroundColor="Transparent" 
                                            Grid.Column="1"
                                            WidthRequest="24"
                                            HeightRequest="24"
                                            Padding="0"
                                            HorizontalOptions="End"
                                            VerticalOptions="Center"
                                            Command="{Binding CloseSubcategoryDropdownCommand}"/>
                                </Grid>
                                <ScrollView Grid.Row="1"
                                          MaximumHeightRequest="120">
                                    <CollectionView ItemsSource="{Binding CoffeeSubcategories}"
                                                  SelectionMode="None">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate>
                                            <Frame BackgroundColor="#F8E8D7" 
                                                   CornerRadius="0" 
                                                   Padding="10,8" 
                                                   HasShadow="False"
                                                   Margin="0,0,0,1"
                                                   MinimumHeightRequest="36">
                                                <Frame.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Source={x:Reference CoffeeSubcategoryButton}, Path=BindingContext.SelectSubcategoryCommand}" 
                                                                          CommandParameter="{Binding .}"/>
                                                </Frame.GestureRecognizers>
                                                <Label Text="{Binding .}" 
                                                       FontSize="13" 
                                                       FontFamily="OpenSansSemibold"
                                                       TextColor="#5B4F45"
                                                       VerticalOptions="Center"
                                                       VerticalTextAlignment="Center"/>
                                            </Frame>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                    </CollectionView>
                                </ScrollView>
                            </Grid>
                        </Frame>
                    </AbsoluteLayout>
                    <Label Text="{Binding SubcategoryValidationMessage}"
                           TextColor="#D9534F"
                           FontSize="12"
                           Margin="5,-5,0,5"
                           IsVisible="{Binding IsCoffeeSubcategoryVisible}">
                        <Label.Triggers>
                            <DataTrigger TargetType="Label" Binding="{Binding SubcategoryValidationMessage, Converter={StaticResource StringToBoolConverter}}" Value="False">
                                <Setter Property="IsVisible" Value="False"/>
                            </DataTrigger>
                        </Label.Triggers>
                    </Label>

                    <!-- PRODUCT IMAGE -->
                    <Label Text="PRODUCT IMAGE" FontAttributes="Bold" FontSize="16" FontFamily="OpenSansSemibold" TextColor="#5B4F45" Margin="0,20,0,5" />
                    <Frame CornerRadius="12"
                           BackgroundColor="White"
                           HasShadow="False"
                           Padding="20"
                           WidthRequest="400"
                           HeightRequest="200">

                        <Grid>
                            <!-- Preview when image selected -->
                            <Grid IsVisible="{Binding SelectedImageSource, Converter={StaticResource NullToBoolConverter}}">
                                <Grid.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding PickImageCommand}" />
                                </Grid.GestureRecognizers>
                                
                                <Image Source="{Binding SelectedImageSource}"
                                       Aspect="AspectFill"
                                       WidthRequest="360"
                                       HeightRequest="160" />

                                <ImageButton Source="close.png"
                                             WidthRequest="28"
                                             HeightRequest="28"
                                             BackgroundColor="#80000000"
                                             CornerRadius="14"
                                             HorizontalOptions="End"
                                             VerticalOptions="Start"
                                             Margin="6"
                                             Command="{Binding RemoveImageCommand}" />
                            </Grid>

                            <!-- Upload placeholder -->
                            <VerticalStackLayout HorizontalOptions="Center"
                                                 VerticalOptions="Center"
                                                 Spacing="10"
                                                 IsVisible="{Binding SelectedImageSource, Converter={StaticResource NullToInvertedBoolConverter}}">
                                <Image Source="uploadicon.png"
                                       WidthRequest="40"
                                       HeightRequest="40"
                                       Opacity="0.85" />
                                <Label Text="Choose a file or drag and drop it here"
                                       FontSize="14" FontFamily="OpenSans-Semibold"
                                       FontAttributes="Bold"
                                       TextColor="#8B7355"
                                       HorizontalTextAlignment="Center" />
                                <Label Text="JPEG, PNG (max 3MB)"
                                       FontSize="12" FontFamily="OpenSans-Regular"
                                       TextColor="#8B7355"
                                       HorizontalTextAlignment="Center" />
                                <Button Text="Browse File"
                                        Command="{Binding PickImageCommand}"
                                        WidthRequest="120"
                                        HeightRequest="36"
                                        CornerRadius="18"
                                        FontSize="14" FontFamily="OpenSans-Semibold"
                                        Padding="0"
                                        BackgroundColor="#BDAEA2"
                                        TextColor="#5B4F45"
                                        FontAttributes="Bold" />
                            </VerticalStackLayout>

                            <Button BackgroundColor="Transparent"
                                    Command="{Binding PickImageCommand}"
                                    IsVisible="{Binding SelectedImageSource, Converter={StaticResource NullToInvertedBoolConverter}}"
                                    HorizontalOptions="Fill"
                                    VerticalOptions="Fill" />
                        </Grid>
                    </Frame>

                </VerticalStackLayout>

                <!-- VERTICAL SEPARATOR -->
                <BoxView Grid.Column="1"
                         WidthRequest="2"
                         Color="#C9A87C"
                         VerticalOptions="Fill"
                         HorizontalOptions="Center"/>

                <!-- RIGHT PANEL -->
                <ScrollView Grid.Column="2" Padding="30" BackgroundColor="#F8E8D7">
                    <VerticalStackLayout Spacing="15" BackgroundColor="#F8E8D7" Padding="20,0,0,0">

                        <!-- HEADER -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Label Text="Product Pricing" FontSize="28" FontFamily="Montserrat-Black" FontAttributes="Bold" Grid.Column="0" TextColor="#5B4F45" />
                            <ImageButton Source="close.png"
                                         WidthRequest="25"
                                         HeightRequest="25"
                                         Grid.Column="1"
                                         VerticalOptions="Center"
                                         Command="{Binding CloseAddItemToPOSCommand}"/>
                        </Grid>

                        <!-- PRICE INPUTS -->
                        <Label Text="SET PRODUCT PRICES" FontAttributes="Bold" FontSize="16" FontFamily="OpenSansSemibold" TextColor="#5B4F45" Margin="0,10,0,5"/>

                        <HorizontalStackLayout Spacing="15">
                            <!-- Small price - only visible for Coffee category -->
                            <VerticalStackLayout IsVisible="{Binding IsSmallPriceVisible}">
                                <Label Text="SMALL" FontAttributes="Bold" FontSize="14" FontFamily="OpenSansSemibold" TextColor="#5B4F45" Margin="0,0,0,5"/>
                                <Border Stroke="{Binding SmallPriceValidationMessage, Converter={StaticResource ValidationBorderColorConverter}}" 
                                        StrokeShape="RoundRectangle 10" 
                                        StrokeThickness="2"
                                        WidthRequest="140"
                                        HeightRequest="55">
                                    <Entry Placeholder="Small Price"
                                           Text="{Binding SmallPrice}"
                                           Keyboard="Numeric"
                                           TextColor="#5B4F45"
                                           PlaceholderColor="#8B7355"
                                           FontSize="15"
                                           BackgroundColor="Transparent"
                                           Margin="12,0"/>
                                </Border>
                                <Label Text="{Binding SmallPriceValidationMessage}"
                                       TextColor="#D9534F"
                                       FontSize="11"
                                       Margin="0,2,0,0"
                                       IsVisible="{Binding SmallPriceValidationMessage, Converter={StaticResource StringToBoolConverter}}"/>
                            </VerticalStackLayout>

                            <VerticalStackLayout>
                                <Label Text="MEDIUM" FontAttributes="Bold" FontSize="14" FontFamily="OpenSansSemibold" TextColor="#5B4F45" Margin="0,0,0,5"/>
                                <Border Stroke="{Binding MediumPriceValidationMessage, Converter={StaticResource ValidationBorderColorConverter}}" 
                                        StrokeShape="RoundRectangle 10" 
                                        StrokeThickness="2"
                                        WidthRequest="140"
                                        HeightRequest="55">
                                    <Entry Placeholder="Medium Price"
                                           Text="{Binding MediumPrice}"
                                           Keyboard="Numeric"
                                           TextColor="#5B4F45"
                                           PlaceholderColor="#8B7355"
                                           FontSize="15"
                                           BackgroundColor="Transparent"
                                           Margin="12,0"/>
                                </Border>
                                <Label Text="{Binding MediumPriceValidationMessage}"
                                       TextColor="#D9534F"
                                       FontSize="11"
                                       Margin="0,2,0,0"
                                       IsVisible="{Binding MediumPriceValidationMessage, Converter={StaticResource StringToBoolConverter}}"/>
                            </VerticalStackLayout>

                            <VerticalStackLayout>
                                <Label Text="LARGE" FontAttributes="Bold" FontSize="14" FontFamily="OpenSansSemibold" TextColor="#5B4F45" Margin="0,0,0,5"/>
                                <Border Stroke="{Binding LargePriceValidationMessage, Converter={StaticResource ValidationBorderColorConverter}}" 
                                        StrokeShape="RoundRectangle 10" 
                                        StrokeThickness="2"
                                        WidthRequest="140"
                                        HeightRequest="55">
                                    <Entry Placeholder="Large Price"
                                           Text="{Binding LargePrice}"
                                           Keyboard="Numeric"
                                           TextColor="#5B4F45"
                                           PlaceholderColor="#8B7355"
                                           FontSize="15"
                                           BackgroundColor="Transparent"
                                           Margin="12,0"/>
                                </Border>
                                <Label Text="{Binding LargePriceValidationMessage}"
                                       TextColor="#D9534F"
                                       FontSize="11"
                                       Margin="0,2,0,0"
                                       IsVisible="{Binding LargePriceValidationMessage, Converter={StaticResource StringToBoolConverter}}"/>
                            </VerticalStackLayout>
                        </HorizontalStackLayout>

                        <!-- DESCRIPTION -->
                        <Label Text="DESCRIPTION (OPTIONAL)" FontAttributes="Bold" FontSize="18" TextColor="#5B4F45" Margin="0,20,0,10"/>
                        <Frame BackgroundColor="White" CornerRadius="15" Padding="15" HasShadow="False" BorderColor="#8B7355">
                            <Editor Placeholder="Enter product description..."
                                    Text="{Binding ProductDescription}"
                                    HeightRequest="120"
                                    FontSize="16"
                                    BackgroundColor="Transparent"
                                    TextColor="#5B4F45"
                                    PlaceholderColor="#8B7355"/>
                        </Frame>

                        <!-- General Validation Message -->
                        <Frame IsVisible="{Binding GeneralValidationMessage, Converter={StaticResource StringToBoolConverter}}" 
                               BackgroundColor="#FFE8E8" 
                               Padding="15,10" 
                               CornerRadius="10" 
                               Margin="0,10,0,0"
                               HasShadow="False">
                            <StackLayout Orientation="Horizontal" Spacing="8">
                                <Label Text="✗" TextColor="#D9534F" FontSize="14" FontFamily="MontserratBlack"/>
                                <Label Text="{Binding GeneralValidationMessage}" FontSize="13" TextColor="#D9534F" FontFamily="MontserratBlack"/>
                            </StackLayout>
                        </Frame>

                        <!-- BUTTONS -->
                        <HorizontalStackLayout HorizontalOptions="End" Spacing="20" Margin="0,20,0,0">
                            <Button Text="Cancel"
                                    WidthRequest="130"
                                    HeightRequest="50"
                                    BackgroundColor="#BDAEA2"
                                    TextColor="#5B4F45"
                                    BorderColor="#8B7355"
                                    BorderWidth="2"
                                    Command="{Binding CloseAddItemToPOSCommand}"
                                    CornerRadius="20"
                                    FontSize="15"
                                    Padding="0"
                                    FontAttributes="Bold"/>

                            <Button Text="Save"
                                    WidthRequest="130"
                                    HeightRequest="50"
                                    BackgroundColor="#D4A574"
                                    TextColor="White"
                                    Command="{Binding SaveProductCommand}"
                                    CornerRadius="20"
                                    FontSize="15"
                                    Padding="0"
                                    FontAttributes="Bold"
                                    IsVisible="{Binding IsEditMode}"/>

                            <Button Text="Next"
                                    WidthRequest="130"
                                    HeightRequest="50"
                                    BackgroundColor="#5B4F45"
                                    TextColor="White"
                                    Command="{Binding OpenConnectPOSToInventoryCommand}"
                                    CornerRadius="20"
                                    FontSize="15"
                                    Padding="0"
                                    FontAttributes="Bold"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </ScrollView>
            </Grid>
        </Frame>
        
    </AbsoluteLayout>
</ContentView>
