<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Coftea_Capstone.Views.Controls.AddItemToPOS"
             xmlns:views="clr-namespace:Coftea_Capstone.Views.Pages"
             xmlns:otherViews="clr-namespace:Coftea_Capstone.Views.Controls"
             BackgroundColor="#80000000"
             IsVisible="{Binding IsAddItemToPOSVisible}"
             InputTransparent="False">

    <!-- Add Item to POS Section -->
    <Grid ColumnDefinitions="200, *" RowDefinitions="*" InputTransparent="False">

        <!-- Background Gradient -->
        <VerticalStackLayout Grid.Column="1" Padding="30,60,0,0">
            <HorizontalStackLayout Spacing="20">
                <Frame MaximumHeightRequest="550" HeightRequest="600" WidthRequest="500" BackgroundColor="#F8E8D7">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="Product Details" FontAttributes="Bold" FontSize="30" Padding="0,0,0,25"/>
                        <Label Text="Product Name" FontSize="25"/>
                        <Frame BackgroundColor="#FFFFFF" CornerRadius="10" Padding="10" HasShadow="False">
                            <Entry Placeholder="Product Name" PlaceholderColor="Black" Text="{Binding ProductName}" Visual="Default" BackgroundColor="Transparent"/>
                        </Frame>

                        <Frame BorderColor="Gray" CornerRadius="10" HeightRequest="150" WidthRequest="455" BackgroundColor="#E0E0E0"
                               HorizontalOptions="Start" Margin="0,15,0,15">
                            <Grid>
                                <Button BackgroundColor="Transparent" Command="{Binding PickImageCommand}" />
                                <Image Source="{Binding SelectedImageSource}" Aspect="AspectFit" />
                                
                            </Grid>
                        </Frame>

                        <Grid ColumnDefinitions="*, *" Margin="0,0,0,0">
                            <VerticalStackLayout Spacing="5" Grid.Column="0">
                                <Label Text="Category" FontSize="25"/>
                                <Picker Title="Select Category"
                                        ItemsSource="{Binding Categories}"
                                        SelectedItem="{Binding SelectedCategory}"
                                        FontSize="14"
                                        HorizontalOptions="Fill"/>
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="5" Grid.Column="1" IsVisible="{Binding IsFruitSodaSubcategoryVisible}">
                                <Label Text="Subcategory" FontSize="25"/>
                                <Picker Title="Select Subcategory"
                                        ItemsSource="{Binding FruitSodaSubcategories}"
                                        SelectedItem="{Binding SelectedSubcategory}"
                                        FontSize="14"
                                        HorizontalOptions="Fill"/>
                            </VerticalStackLayout>
                        </Grid>

                        <Label Text="Color Code" FontSize="25"/>
                        <Frame BackgroundColor="#FFFFFF" CornerRadius="10" Padding="10" HasShadow="False">
                            <Entry Placeholder="Product Category" Keyboard="Email" PlaceholderColor="Black" Visual="Default" BackgroundColor="Transparent"/>
                        </Frame>
                    </VerticalStackLayout>
                </Frame>
                <Frame MaximumHeightRequest="550" HeightRequest="600" WidthRequest="500" BackgroundColor="#F8E8D7">
                    <VerticalStackLayout>
                        <Grid Margin="0,0,0,25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Label Text="Product Size" FontAttributes="Bold" FontSize="30" VerticalOptions="Center" Grid.Column="0"/>
                            <ImageButton Source="close.png" WidthRequest="20" HeightRequest="20" Grid.Column="1" 
                                         VerticalOptions="Center" Command="{Binding CloseAddItemToPOSCommand}"/>
                        </Grid>

                        <Grid ColumnDefinitions="*, *" ColumnSpacing="20" Margin="0,10,0,0">
                            <VerticalStackLayout Spacing="5" Grid.Column="0">
                                <Label Text="Small Price" FontSize="25"/>
                                <Frame BackgroundColor="#FFFFFF" CornerRadius="10" Padding="10" HasShadow="False">
                                    <Entry Placeholder="Enter Small Price"
                                           Keyboard="Numeric"
                                           Text="{Binding SmallPrice}"
                                           PlaceholderColor="Black"
                                           Visual="Default"
                                           BackgroundColor="Transparent" />
                                </Frame>
                            </VerticalStackLayout>
                            <VerticalStackLayout Spacing="5" Grid.Column="1">
                                <Label Text="Large Price" FontSize="25"/>
                                <Frame BackgroundColor="#FFFFFF" CornerRadius="10" Padding="10" HasShadow="False">
                                    <Entry Placeholder="Enter Large Price"
                                           Keyboard="Numeric"
                                           Text="{Binding LargePrice}"
                                           PlaceholderColor="Black"
                                           Visual="Default"
                                           BackgroundColor="Transparent" /> 
                                </Frame>
                            </VerticalStackLayout>
                        </Grid>
                        
                        <Label Text="Description (Optional)" FontSize="25" Margin="0,25,0,0"/>
                        <Frame BackgroundColor="#FFFFFF" 
                               Margin="0,10,0,0"
                               CornerRadius="10" 
                               Padding="10"
                               HasShadow="False">
                            <Editor Placeholder="Enter product description..." 
                                HeightRequest="150" 
                                Text="{Binding ProductDescription}" 
                                BackgroundColor="Transparent"
                                TextColor="Black"/>
                        </Frame>
                        <HorizontalStackLayout HorizontalOptions="End" Spacing="15">
                            <Button Text="Next" WidthRequest="150" BackgroundColor="#E6DE9D" TextColor="Black" 
                                Command="{Binding OpenConnectPOSToInventoryCommand}" HorizontalOptions="End" Margin="0,20,0,0"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Frame>
            </HorizontalStackLayout>
        </VerticalStackLayout>
        <otherViews:ConnectPOSItemToInventory
                  BindingContext="{Binding ConnectPOSToInventoryVM}" Grid.Column="2"/>
        <otherViews:InputIngredientsAmountUsed
                  BindingContext="{Binding ConnectPOSToInventoryVM}" Grid.Column="2"/>

    </Grid>
</ContentView>