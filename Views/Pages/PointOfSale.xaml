<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Coftea_Capstone.Views.Pages.PointOfSale"
             xmlns:vm="clr-namespace:Coftea_Capstone.ViewModel"
             xmlns:view="clr-namespace:Coftea_Capstone.Views.Pages"
             xmlns:otherViews="clr-namespace:Coftea_Capstone.Views.Controls"
             xmlns:converters="clr-namespace:Coftea_Capstone.ViewModel.Others"
             Shell.NavBarIsVisible="False"      
             Shell.TabBarIsVisible="False"
             x:Name="POS"
             BackgroundColor="#D6BCA8">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:RadioButtonItemSize x:Key="RadioButtonItemSize"/>
            <converters:NullToBoolConverter x:Key="NullToBoolConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <Grid ColumnDefinitions="200, *" RowDefinitions="*">
        
        <!-- Sidebar Navigation -->
        <view:NavigationBar Grid.Column="0" />

        <!-- Main Content -->
        <Grid Grid.Column="1" Padding="20" RowDefinitions="Auto, Auto, *">

            <!-- Top Bar -->
            <Grid ColumnDefinitions="*, Auto, Auto, Auto, Auto" VerticalOptions="Center" ColumnSpacing="15" Grid.Row="0">
                <Label Text="Point Of Sale" FontSize="28" FontAttributes="Bold" FontFamily="MontserratBlack" VerticalOptions="Center"/>
                <Label x:Name="TimerLabel" Grid.Column="1" VerticalOptions="Center" FontSize="18" FontAttributes="Bold" TextColor="Black" />
                <SearchBar Placeholder="Search for something" Grid.Column="2" WidthRequest="250" HorizontalOptions="FillAndExpand"/>
                <ImageButton Source="notification.png" Grid.Column="3" WidthRequest="35" HeightRequest="28" Command="{Binding SettingsPopup.ShowSettingsPopupCommand}" />
                <ImageButton Source="usericon.png" Grid.Column="4" WidthRequest="35" HeightRequest="35"/>
            </Grid>

            <!-- Menu Categories -->
            <ScrollView Orientation="Horizontal" Padding="0,15,0,0" HorizontalOptions="Center" Grid.Row="1">
                <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
                    <Button Text="Frappe" FontAttributes="Bold" FontFamily="Roboto" TextColor="Black" FontSize="14" 
                            BackgroundColor="#897564" WidthRequest="120" HeightRequest="40"  
                            Command="{Binding FilterByCategoryCommand}" CommandParameter="Frappe"/>
                    <Button Text="Fruit/Soda" FontAttributes="Bold" FontFamily="Roboto" TextColor="Black" FontSize="14"
                            BackgroundColor="#897564" WidthRequest="120" HeightRequest="40"
                            Command="{Binding FilterByCategoryCommand}" CommandParameter="Fruit/Soda"/>
                    <Button Text="Milktea" FontAttributes="Bold" FontFamily="Roboto" TextColor="Black" FontSize="14"
                            BackgroundColor="#897564" WidthRequest="120" HeightRequest="40"
                            Command="{Binding FilterByCategoryCommand}" CommandParameter="Milktea"/>
                    <Button Text="Coffee" FontAttributes="Bold" FontFamily="Roboto" TextColor="Black" FontSize="14" 
                            BackgroundColor="#897564" WidthRequest="120" HeightRequest="40" 
                            Command="{Binding FilterByCategoryCommand}" CommandParameter="Coffee"/>
                </HorizontalStackLayout>
            </ScrollView>

            <!-- Products + Cart -->
            <Grid ColumnDefinitions="3*,2*" RowDefinitions="*" ColumnSpacing="15" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" Grid.Row="2">
                <VerticalStackLayout Grid.ColumnSpan="2" HorizontalOptions="Center" VerticalOptions="Start">
                    <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" />
                    <Label Text="{Binding StatusMessage}" TextColor="{Binding HasError}" FontSize="14" HorizontalOptions="Center" />
                </VerticalStackLayout>
                <!-- Products -->
                <Border Grid.Column="0" StrokeShape="RoundRectangle 20" BackgroundColor="White" Margin="0,25,0,0"
                        VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                    <CollectionView ItemsSource="{Binding FilteredProducts}">
                        <CollectionView.ItemsLayout>
                            <GridItemsLayout Orientation="Vertical" Span="3" />
                        </CollectionView.ItemsLayout>

                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame BackgroundColor="#D9D9D9" CornerRadius="20" Margin="10" Padding="5" VerticalOptions="Fill" HorizontalOptions="Fill">
                                    <Frame.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding BindingContext.SelectProductCommand, Source={x:Reference POS}}" CommandParameter="{Binding .}" />
                                    </Frame.GestureRecognizers>
                                    <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center">
                                        <Frame CornerRadius="20" Padding="0">
                                            <!-- Bind to ImageSource property -->
                                            <Image Source="{Binding ImageSource}" Aspect="AspectFill" HorizontalOptions="Fill" VerticalOptions="Fill"/>
                                        </Frame>
                                        <Frame CornerRadius="15" BackgroundColor="White" Padding="5,2" Margin="0,5,0,0" HasShadow="False" HorizontalOptions="Center">
                                            <Label Text="{Binding ProductName}" FontAttributes="Bold" FontSize="14" HorizontalOptions="Center" VerticalOptions="Center"/>
                                        </Frame>
                                        <HorizontalStackLayout HorizontalOptions="Center">
                                            <Label Text="{Binding SmallPrice, StringFormat='₱{0:F2}'}" FontSize="13"/>
                                            <Label Text=" | " FontSize="13"/>
                                            <Label Text="{Binding LargePrice, StringFormat='₱{0:F2}'}" FontSize="13"/>
                                        </HorizontalStackLayout>
                                    </VerticalStackLayout>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Border>

                <!-- Cart -->
                <Frame Grid.Column="1" CornerRadius="20" BackgroundColor="White" Margin="0,25,0,0"
                       VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
                    <ScrollView>
                        <VerticalStackLayout Padding="15" Spacing="15" IsVisible="{Binding SelectedProduct, Converter={StaticResource NullToBoolConverter}}">

                            <!-- Product Image -->
                            <Frame CornerRadius="20" Padding="0" HeightRequest="150" WidthRequest="150" HorizontalOptions="Center">
                                <Image Source="{Binding SelectedProduct.ImageSource}" Aspect="AspectFill"/>
                            </Frame>

                            <!-- Product Name -->
                            <Label Text="{Binding SelectedProduct.ProductName}" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center"/>

                            <!-- Addons -->

                            <!-- Size Selection -->
                            <Label Text="Select Size:" FontSize="16" FontAttributes="Bold"/>
                            <HorizontalStackLayout Spacing="15">
                                <RadioButton Content="Medium - ₱59" GroupName="SizeGroup" Value="Medium" IsChecked="{Binding SelectedProduct.SelectedSize, ConverterParameter=Medium}" />
                                <RadioButton Content="Large - ₱69" GroupName="SizeGroup" Value="Large" IsChecked="{Binding SelectedProduct.SelectedSize, ConverterParameter=Large}" />
                            </HorizontalStackLayout>

                            <!-- Customer Name -->
                            <Label Text="Customer Name:" FontSize="16" FontAttributes="Bold"/>
                            <Entry Text="{Binding CustomerName}" Placeholder="Enter name"/>

                            <!-- Quantity Selector -->
                            <Label Text="Drinks Qty:" FontSize="16" FontAttributes="Bold"/>
                            <HorizontalStackLayout Spacing="10">
                                <Button Text="-" Command="{Binding DecreaseQuantityCommand}" WidthRequest="40" HeightRequest="40"/>
                                <Label Text="{Binding SelectedProduct.Quantity}" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" WidthRequest="40"/>
                                <Button Text="+" Command="{Binding IncreaseQuantityCommand}" WidthRequest="40" HeightRequest="40"/>
                            </HorizontalStackLayout>

                            <!-- Total Price -->
                            <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
                                <Label Text="Total:" FontSize="18" FontAttributes="Bold"/>
                                <Label Text="{Binding SelectedProduct.TotalPrice, StringFormat='₱ {0:F2}'}" FontSize="18" FontAttributes="Bold"/>
                            </HorizontalStackLayout>

                            <!-- Action Buttons -->
                            <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
                                <Button Text="Cancel Order" Command="{Binding RemoveFromCartCommand}" CommandParameter="{Binding SelectedProduct}" TextColor="White" WidthRequest="130" HeightRequest="50">
                                    <Button.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#A57C5C" Offset="0.0" />
                                            <GradientStop Color="#5B4F45" Offset="1.0" />
                                        </LinearGradientBrush>
                                    </Button.Background>
                                    <Button.CornerRadius>25</Button.CornerRadius>
                                </Button>
                                <Button Text="Add Order" Command="{Binding AddToCartCommand}" WidthRequest="130" FontAttributes="Bold" TextColor="White" HeightRequest="50">
                                    <Button.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#A57C5C" Offset="0.0" />
                                            <GradientStop Color="#5B4F45" Offset="1.0" />
                                        </LinearGradientBrush>
                                    </Button.Background>
                                    <Button.CornerRadius>25</Button.CornerRadius>
                                </Button>
                            </HorizontalStackLayout>

                        </VerticalStackLayout>
                    </ScrollView>
                </Frame>

            </Grid>
        </Grid>
        <!-- Popups -->
        <view:Settings BindingContext="{Binding SettingsPopup}" Grid.ColumnSpan="2"/>
        <otherViews:AddItemToPOS BindingContext="{Binding AddItemToPOSViewModel}" Grid.ColumnSpan="2"/>
    </Grid>
</ContentPage>
