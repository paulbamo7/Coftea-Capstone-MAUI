<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="Coftea_Capstone.Views.Pages.SalesReport" 
             xmlns:vm="clr-namespace:Coftea_Capstone.ViewModel" 
             xmlns:views="clr-namespace:Coftea_Capstone.Views.Pages"
             xmlns:charts="clr-namespace:Coftea_Capstone.Views.Controls"
             xmlns:otherViews="clr-namespace:Coftea_Capstone.Views.Controls" 
             xmlns:converters="clr-namespace:Coftea_Capstone.ViewModel.Others"
             Shell.TabBarIsVisible="False" 
             BackgroundColor="#C1A892">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:InvertBoolConverter x:Key="InvertBoolConverter"/>
            <converters:StringToColorConverter x:Key="StringToColorConverter"/>
            
            <!-- Style for Time Period Buttons -->
            <Style x:Key="TimePeriodButtonStyle" TargetType="Button">
                <Setter Property="FontSize" Value="8"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="TextColor" Value="#2E7D32"/>
                <Setter Property="BackgroundColor" Value="White"/>
                <Setter Property="HeightRequest" Value="32"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="BorderColor" Value="#C8E6C9"/>
                <Setter Property="BorderWidth" Value="1"/>
                <Setter Property="Padding" Value="3,0"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="*">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="SidebarColumn" Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <views:NavigationBar x:Name="Sidebar" Grid.Column="0" />

        <!-- Main Content -->
        <ScrollView Grid.Column="1">
            <VerticalStackLayout Padding="20" Spacing="16">

                <!-- Top Bar -->
                <Grid ColumnDefinitions="*,Auto,Auto" Padding="0,10" VerticalOptions="Center" ColumnSpacing="16">
                    <Label Text="Sales Report" FontSize="32" FontAttributes="Bold" FontFamily="MontserratBlack" VerticalOptions="Center" TextColor="Black"/>
                    <ImageButton Grid.Column="1" Source="notification.png" WidthRequest="35" HeightRequest="35" BackgroundColor="Transparent" Clicked="OnBellClicked"/>
                    <Grid Grid.Column="2" WidthRequest="40" HeightRequest="40">
                        <Image Source="{Binding Source={x:Static Application.Current}, Path=ProfilePopup.ProfileImageSource}" 
                               WidthRequest="40" 
                               HeightRequest="40" 
                               Aspect="AspectFill" 
                               ZIndex="1" />
                        <ImageButton Source="usericon.png" 
                                     WidthRequest="40" 
                                     HeightRequest="40" 
                                     BackgroundColor="Transparent"
                                     CornerRadius="20"
                                     Clicked="OnProfileClicked"
                                     ZIndex="0"/>
                    </Grid>
                </Grid>

                <!-- Date Filter Button -->
                <!-- Categories - Centered above pie chart and KPI cards -->
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                    <ScrollView Grid.Column="1" Orientation="Horizontal" HorizontalOptions="Center">
                        <StackLayout Orientation="Horizontal" Spacing="10">
                        <Button Text="Overview" FontAttributes="Bold" TextColor="Black" FontSize="14"
                                BackgroundColor="#FFEAD6" WidthRequest="120" HeightRequest="40"
                                Command="{Binding SelectCategoryCommand}" CommandParameter="Overview">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding SelectedCategory}" Value="Overview">
                                    <Setter Property="BackgroundColor" Value="#D4A574" />
                                    <Setter Property="TextColor" Value="#2b2b2b" />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                        <Button Text="Frappe" FontAttributes="Bold" TextColor="Black" FontSize="14"
                                BackgroundColor="#FFEAD6" WidthRequest="120" HeightRequest="40"
                                Command="{Binding SelectCategoryCommand}" CommandParameter="Frappe">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding SelectedCategory}" Value="Frappe">
                                    <Setter Property="BackgroundColor" Value="#D4A574" />
                                    <Setter Property="TextColor" Value="#2b2b2b" />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                        <Button Text="Fruit/Soda" FontAttributes="Bold" TextColor="Black" FontSize="13"
                                BackgroundColor="#FFEAD6" WidthRequest="140" HeightRequest="40"
                                Command="{Binding SelectCategoryCommand}" CommandParameter="Fruit/Soda">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding SelectedCategory}" Value="Fruit/Soda">
                                    <Setter Property="BackgroundColor" Value="#D4A574" />
                                    <Setter Property="TextColor" Value="#2b2b2b" />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                        <Button Text="Milktea" FontAttributes="Bold" TextColor="Black" FontSize="14"
                                BackgroundColor="#FFEAD6" WidthRequest="120" HeightRequest="40"
                                Command="{Binding SelectCategoryCommand}" CommandParameter="Milktea">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding SelectedCategory}" Value="Milktea">
                                    <Setter Property="BackgroundColor" Value="#D4A574" />
                                    <Setter Property="TextColor" Value="#2b2b2b" />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                        <Button Text="Coffee" FontAttributes="Bold" TextColor="Black" FontSize="14"
                                BackgroundColor="#FFEAD6" WidthRequest="120" HeightRequest="40"
                                Command="{Binding SelectCategoryCommand}" CommandParameter="Coffee">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding SelectedCategory}" Value="Coffee">
                                    <Setter Property="BackgroundColor" Value="#D4A574" />
                                    <Setter Property="TextColor" Value="#2b2b2b" />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>
                        </StackLayout>
                    </ScrollView>
                    <Button Grid.Column="2"
                            Text="{Binding DateFilterText, TargetNullValue='Select Date Range', FallbackValue='Select Date Range'}"
                            FontSize="12"
                            FontAttributes="Bold"
                            TextColor="White"
                            BackgroundColor="#8B7355"
                            CornerRadius="10"
                            Padding="12,6"
                            HeightRequest="40"
                            Command="{Binding SelectDateFilterCommand}"
                            HorizontalOptions="End"
                            VerticalOptions="Center"/>
                </Grid>

                <!-- KPI Cards, Pie Chart, and Total Accumulated - Three column layout -->
                <Grid ColumnDefinitions="*,*,*" ColumnSpacing="12">
                    <!-- Left Column: KPI Cards -->
                    <VerticalStackLayout Grid.Column="0" Spacing="12" VerticalOptions="Start">
                        <Frame BackgroundColor="#F5E6D8" CornerRadius="12" HeightRequest="110" Padding="12" HasShadow="True">
                            <VerticalStackLayout>
                                <Label Text="Overall Most Bought Today" FontAttributes="Bold" FontSize="14" TextColor="#5B4F45"/>
                                <Label Text="{Binding MostBoughtToday}" FontSize="20" TextColor="#5B4F45"/>
                            </VerticalStackLayout>
                        </Frame>

                        <!-- Payment Method Breakdown -->
                        <Grid ColumnDefinitions="*,*,*" ColumnSpacing="8">
                            <!-- Cash Payment -->
                            <Frame BackgroundColor="#E8F5E8" CornerRadius="12" HeightRequest="100" Padding="8" HasShadow="True" Grid.Column="0">
                                <VerticalStackLayout Spacing="2" HorizontalOptions="Center" VerticalOptions="Center">
                                    <Label Text="Cash" FontAttributes="Bold" FontSize="14" TextColor="#2E7D32"/>
                                    <Label Text="{Binding DisplayCashTotal, StringFormat='₱{0:N2}'}" FontSize="18" TextColor="#2E7D32" FontAttributes="Bold" LineBreakMode="NoWrap"/>
                                </VerticalStackLayout>
                            </Frame>

                            <!-- GCash Payment -->
                            <Frame BackgroundColor="#E3F2FD" CornerRadius="12" HeightRequest="100" Padding="8" HasShadow="True" Grid.Column="1">
                                <VerticalStackLayout Spacing="2" HorizontalOptions="Center" VerticalOptions="Center">
                                    <Label Text="GCash" FontAttributes="Bold" FontSize="14" TextColor="#1976D2"/>
                                    <Label Text="{Binding DisplayGCashTotal, StringFormat='₱{0:N2}'}" FontSize="18" TextColor="#1976D2" FontAttributes="Bold" LineBreakMode="NoWrap"/>
                                </VerticalStackLayout>
                            </Frame>

                            <!-- Bank Payment -->
                            <Frame BackgroundColor="#F3E5F5" CornerRadius="12" HeightRequest="100" Padding="8" HasShadow="True" Grid.Column="2">
                                <VerticalStackLayout Spacing="2" HorizontalOptions="Center" VerticalOptions="Center">
                                    <Label Text="Bank" FontAttributes="Bold" FontSize="14" TextColor="#7B1FA2"/>
                                    <Label Text="{Binding DisplayBankTotal, StringFormat='₱{0:N2}'}" FontSize="18" TextColor="#7B1FA2" FontAttributes="Bold" LineBreakMode="NoWrap"/>
                                </VerticalStackLayout>
                            </Frame>
                        </Grid>
                    </VerticalStackLayout>

                    <!-- Middle Column: Top 3 Picks Today - Pie Chart -->
                    <Frame Grid.Column="1" BackgroundColor="White" CornerRadius="12" Padding="12" HasShadow="True" HeightRequest="300" VerticalOptions="Start">
                        <Grid RowDefinitions="Auto,*,Auto">
                            <!-- Header -->
                            <Grid ColumnDefinitions="*,Auto">
                            <Label Text="Top 3 Picks Today" FontAttributes="Bold" FontSize="18" TextColor="#5B4F45"/>
                            <Label Text="{Binding FilteredTodayOrders, StringFormat='{0} Orders'}" Grid.Column="1" FontSize="14" TextColor="Gray"/>
                            </Grid>

                            <!-- Pie left, Names right -->
                            <Grid Grid.Row="1" ColumnDefinitions="Auto,*" ColumnSpacing="16" VerticalOptions="Start">
                                <otherViews:SimplePieChart Grid.Column="0"
                                                          HorizontalOptions="Start"
                                                          VerticalOptions="Center"
                                                          HeightRequest="160"
                                                          WidthRequest="160"
                                                          ShowLegend="False"
                                                          ItemsSource="{Binding TopItemsToday}" 
                                                      Item1Name="{Binding Item1Name}" 
                                                      Item2Name="{Binding Item2Name}" 
                                                      Item3Name="{Binding Item3Name}"
                                                      Item1Count="{Binding Item1Count}"
                                                      Item2Count="{Binding Item2Count}"
                                                      Item3Count="{Binding Item3Count}"
                                                      Item1HasTrend="{Binding Item1HasTrend}"
                                                      Item2HasTrend="{Binding Item2HasTrend}"
                                                      Item3HasTrend="{Binding Item3HasTrend}"/>

<<<<<<< Updated upstream
                                <VerticalStackLayout Grid.Column="1" Spacing="12" HorizontalOptions="End" VerticalOptions="Start" Margin="8,20,0,0">
                                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto" ColumnSpacing="6">
                                        <BoxView Grid.RowSpan="2" WidthRequest="10" HeightRequest="10" VerticalOptions="Center" Color="#66BB6A"/>
                                        <Label Grid.Column="1" Grid.Row="0" Text="{Binding Item1Name}" FontSize="14" TextColor="#5B4F45"/>
                                        <Label Grid.Column="1" Grid.Row="1" Text="{Binding Item1Count, StringFormat='{0} orders'}" FontSize="12" TextColor="Gray"/>
                                    </Grid>
                                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto" ColumnSpacing="6">
                                        <BoxView Grid.RowSpan="2" WidthRequest="10" HeightRequest="10" VerticalOptions="Center" Color="#8D6E63"/>
                                        <Label Grid.Column="1" Grid.Row="0" Text="{Binding Item2Name}" FontSize="14" TextColor="#5B4F45"/>
                                        <Label Grid.Column="1" Grid.Row="1" Text="{Binding Item2Count, StringFormat='{0} orders'}" FontSize="12" TextColor="Gray"/>
                                    </Grid>
                                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto" ColumnSpacing="6">
                                        <BoxView Grid.RowSpan="2" WidthRequest="10" HeightRequest="10" VerticalOptions="Center" Color="#795548"/>
                                        <Label Grid.Column="1" Grid.Row="0" Text="{Binding Item3Name}" FontSize="14" TextColor="#5B4F45"/>
=======
                                <VerticalStackLayout Grid.Column="1" Spacing="14" HorizontalOptions="Start" VerticalOptions="Center" Margin="8,10,8,10">
                                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto" ColumnSpacing="8" RowSpacing="2">
                                        <BoxView Grid.RowSpan="2" WidthRequest="10" HeightRequest="10" VerticalOptions="Center" Color="#66BB6A"/>
                                        <Label Grid.Column="1" Grid.Row="0" Text="{Binding Item1Name}" FontSize="14" TextColor="#5B4F45" LineBreakMode="WordWrap"/>
                                        <Label Grid.Column="1" Grid.Row="1" Text="{Binding Item1Count, StringFormat='{0} orders'}" FontSize="12" TextColor="Gray"/>
                                    </Grid>
                                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto" ColumnSpacing="8" RowSpacing="2">
                                        <BoxView Grid.RowSpan="2" WidthRequest="10" HeightRequest="10" VerticalOptions="Center" Color="#8D6E63"/>
                                        <Label Grid.Column="1" Grid.Row="0" Text="{Binding Item2Name}" FontSize="14" TextColor="#5B4F45" LineBreakMode="WordWrap"/>
                                        <Label Grid.Column="1" Grid.Row="1" Text="{Binding Item2Count, StringFormat='{0} orders'}" FontSize="12" TextColor="Gray"/>
                                    </Grid>
                                    <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto" ColumnSpacing="8" RowSpacing="2">
                                        <BoxView Grid.RowSpan="2" WidthRequest="10" HeightRequest="10" VerticalOptions="Center" Color="#795548"/>
                                        <Label Grid.Column="1" Grid.Row="0" Text="{Binding Item3Name}" FontSize="14" TextColor="#5B4F45" LineBreakMode="WordWrap"/>
>>>>>>> Stashed changes
                                        <Label Grid.Column="1" Grid.Row="1" Text="{Binding Item3Count, StringFormat='{0} orders'}" FontSize="12" TextColor="Gray"/>
                                    </Grid>
                                </VerticalStackLayout>
                            </Grid>
                        </Grid>
                    </Frame>

                    <!-- Right Column: Total Cash Accumulated -->
<<<<<<< Updated upstream
                    <Frame Grid.Column="2" BackgroundColor="#C8E6C9" CornerRadius="15" Padding="20" HasShadow="True" HeightRequest="300" VerticalOptions="Start" BorderColor="#81C784">
                        <VerticalStackLayout Spacing="8" HorizontalOptions="Fill">
                            <VerticalStackLayout Spacing="6" HorizontalOptions="Center">
=======
                    <Frame Grid.Column="2" BackgroundColor="#C8E6C9" CornerRadius="15" Padding="20" HasShadow="True" VerticalOptions="Start" BorderColor="#81C784">
                        <Grid RowDefinitions="Auto,Auto,Auto,Auto" ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Spacing="6" HorizontalOptions="Center">
>>>>>>> Stashed changes
                                <Label Text="Total Sales" FontAttributes="Bold" FontSize="20" FontFamily="Montserrat-Black" TextColor="#2E7D32" HorizontalOptions="Center"/>
                                <Label Text="{Binding SelectedTimePeriod, StringFormat='{0} sales totals'}" FontSize="13" TextColor="#558B2F" HorizontalOptions="Center"/>
                            </VerticalStackLayout>
                            
                            <!-- Time Period Filter Buttons -->
                            <Grid Grid.Row="1" Grid.ColumnSpan="2" ColumnDefinitions="*,*,*,*,*" ColumnSpacing="2" Margin="0,12,0,0">
                                <Button Text="Today" 
                                        Style="{StaticResource TimePeriodButtonStyle}"
                                        Grid.Column="0"
                                        Command="{Binding SelectTimePeriodCommand}" 
                                        CommandParameter="Today"/>
                                <Button Text="Yesterday" 
                                        Style="{StaticResource TimePeriodButtonStyle}"
                                        Grid.Column="1"
                                        Command="{Binding SelectTimePeriodCommand}" 
                                        CommandParameter="Yesterday"/>
                                <Button Text="1 Week" 
                                        Style="{StaticResource TimePeriodButtonStyle}"
                                        Grid.Column="2"
                                        Command="{Binding SelectTimePeriodCommand}" 
                                        CommandParameter="1 Week"/>
                                <Button Text="1 Month" 
                                        Style="{StaticResource TimePeriodButtonStyle}"
                                        Grid.Column="3"
                                        Command="{Binding SelectTimePeriodCommand}" 
                                        CommandParameter="1 Month"/>
                                <Button Text="All Time" 
                                        Style="{StaticResource TimePeriodButtonStyle}"
                                        Grid.Column="4"
                                        Command="{Binding SelectTimePeriodCommand}" 
                                        CommandParameter="All Time"/>
                            </Grid>
                            
                            <Grid Grid.Row="2" Grid.ColumnSpan="2" ColumnDefinitions="*,*,*" ColumnSpacing="8" Margin="0,12,0,0">
                                <Frame BackgroundColor="#E8F5E8" CornerRadius="10" Padding="10" HasShadow="False" Grid.Column="0">
                                    <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
                                        <Label Text="Cash" FontAttributes="Bold" FontSize="12" TextColor="#2E7D32" HorizontalOptions="Center"/>
                                        <Label Text="{Binding DisplayCashTotal, StringFormat='₱{0:N2}'}" FontSize="14" FontAttributes="Bold" TextColor="#2E7D32" HorizontalOptions="Center"/>
                                    </VerticalStackLayout>
                                </Frame>
                                <Frame BackgroundColor="#E3F2FD" CornerRadius="10" Padding="10" HasShadow="False" Grid.Column="1">
                                    <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
                                        <Label Text="GCash" FontAttributes="Bold" FontSize="12" TextColor="#1976D2" HorizontalOptions="Center"/>
                                        <Label Text="{Binding DisplayGCashTotal, StringFormat='₱{0:N2}'}" FontSize="14" FontAttributes="Bold" TextColor="#1976D2" HorizontalOptions="Center"/>
                                    </VerticalStackLayout>
                                </Frame>
                                <Frame BackgroundColor="#F3E5F5" CornerRadius="10" Padding="10" HasShadow="False" Grid.Column="2">
                                    <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
                                        <Label Text="Bank" FontAttributes="Bold" FontSize="12" TextColor="#7B1FA2" HorizontalOptions="Center"/>
                                        <Label Text="{Binding DisplayBankTotal, StringFormat='₱{0:N2}'}" FontSize="14" FontAttributes="Bold" TextColor="#7B1FA2" HorizontalOptions="Center"/>
                                    </VerticalStackLayout>
                                </Frame>
                            </Grid>

                            <Label Text="{Binding DisplayTotalSales, StringFormat='₱{0:N2}'}" 
                                   Grid.Row="3"
                                   Grid.ColumnSpan="2"
                                   FontSize="28" 
                                   FontAttributes="Bold" 
                                   FontFamily="Montserrat-Black"
                                   TextColor="#2E7D32" 
                                   HorizontalOptions="Center"/>
                            
<<<<<<< Updated upstream
                            <Grid ColumnDefinitions="*,*,*" ColumnSpacing="8" Margin="0,10,0,0">
                                <Frame Grid.Column="0" BackgroundColor="White" CornerRadius="12" Padding="10" HasShadow="True" HorizontalOptions="FillAndExpand">
                                    <VerticalStackLayout Spacing="2" HorizontalOptions="Center" VerticalOptions="Center">
                                        <Label Text="💰" FontSize="12" HorizontalOptions="Center"/>
                                        <Label Text="Cash" FontSize="10" TextColor="#558B2F" FontAttributes="Bold" HorizontalOptions="Center"/>
                                        <Label Text="{Binding DisplayCashTotal, StringFormat='₱{0:N2}'}" 
                                               FontSize="12" 
                                               FontAttributes="Bold" 
                                               TextColor="#2E7D32" 
                                               HorizontalOptions="Center"
                                               LineBreakMode="NoWrap"/>
                                    </VerticalStackLayout>
                                </Frame>
                                <Frame Grid.Column="1" BackgroundColor="White" CornerRadius="12" Padding="10" HasShadow="True" HorizontalOptions="FillAndExpand">
                                    <VerticalStackLayout Spacing="2" HorizontalOptions="Center" VerticalOptions="Center">
                                        <Label Text="📱" FontSize="12" HorizontalOptions="Center"/>
                                        <Label Text="GCash" FontSize="10" TextColor="#558B2F" FontAttributes="Bold" HorizontalOptions="Center"/>
                                        <Label Text="{Binding DisplayGCashTotal, StringFormat='₱{0:N2}'}" 
                                               FontSize="12" 
                                               FontAttributes="Bold" 
                                               TextColor="#2E7D32" 
                                               HorizontalOptions="Center"
                                               LineBreakMode="NoWrap"/>
                                    </VerticalStackLayout>
                                </Frame>
                                <Frame Grid.Column="2" BackgroundColor="White" CornerRadius="12" Padding="10" HasShadow="True" HorizontalOptions="FillAndExpand">
                                    <VerticalStackLayout Spacing="2" HorizontalOptions="Center" VerticalOptions="Center">
                                        <Label Text="🏦" FontSize="12" HorizontalOptions="Center"/>
                                        <Label Text="Bank" FontSize="10" TextColor="#558B2F" FontAttributes="Bold" HorizontalOptions="Center"/>
                                        <Label Text="{Binding DisplayBankTotal, StringFormat='₱{0:N2}'}" 
                                               FontSize="12" 
                                               FontAttributes="Bold" 
                                               TextColor="#2E7D32" 
                                               HorizontalOptions="Center"
                                               LineBreakMode="NoWrap"/>
                                    </VerticalStackLayout>
                                </Frame>
                            </Grid>
                        </VerticalStackLayout>
=======
                            <!-- Comparison details and payment breakdown removed per request -->
                        </Grid>
>>>>>>> Stashed changes
                    </Frame>
                </Grid>

                <!-- Weekly and Monthly Picks - Two-column layout -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                    <!-- Weekly Picks -->
                    <Frame BackgroundColor="White" CornerRadius="12" Padding="12" HasShadow="True">
                        <Grid RowDefinitions="Auto,Auto,Auto,Auto" ColumnDefinitions="*,Auto">
                            <Grid ColumnDefinitions="*,Auto" Grid.Row="0">
                                <Label Text="Weekly Picks" FontAttributes="Bold" FontSize="18" TextColor="#5B4F45"/>
                                <Label Text="{Binding FilteredWeeklyOrders, StringFormat='{0} Orders'}" Grid.Column="1" FontSize="14" TextColor="Gray"/>
                            </Grid>
                            <!-- Bar chart for Weekly items -->
                            <CollectionView Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding TopItemsWeekly}" HeightRequest="180" ItemsLayout="HorizontalList">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <VerticalStackLayout HorizontalOptions="Center" WidthRequest="90" Spacing="6" Padding="0">
                                            <Grid HeightRequest="150" BackgroundColor="Transparent" VerticalOptions="FillAndExpand">
                                                <Border VerticalOptions="End" 
                                                        HeightRequest="{Binding ScaledCount}" 
                                                        WidthRequest="50" 
                                                        Background="{Binding ColorCode, Converter={StaticResource StringToColorConverter}}" 
                                                        StrokeThickness="0"
                                                        Stroke="Transparent">
                                                    <Border.StrokeShape>
                                                        <RoundRectangle CornerRadius="12"/>
                                                    </Border.StrokeShape>
                                                </Border>
                                            </Grid>
                                            <Label Text="{Binding Name}" TextColor="#8B7355" FontSize="11"  LineBreakMode="TailTruncation" HorizontalTextAlignment="Center"/>
                                        </VerticalStackLayout>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                            <CollectionView ItemsSource="{Binding TopItemsWeekly}" HeightRequest="60" Grid.Row="2" Grid.ColumnSpan="2" Margin="0,0,0,0">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnDefinitions="*,Auto">
                                            <Label Text="{Binding Name}" FontSize="12" TextColor="Gray"/>
                                            <Label Text="{Binding Count, StringFormat='{0} sold'}" FontSize="12" TextColor="Gray" Grid.Column="1"/>
                                        </Grid>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                            <!-- PDF Download Button at bottom -->
                            <Button Grid.Row="3" 
                                    Grid.ColumnSpan="2"
                                    Text="📄 Download Weekly Report" 
                                    FontSize="13" 
                                    FontAttributes="Bold"
                                    TextColor="#5B4F45" 
                                    BackgroundColor="#FFB3D9" 
                                    HeightRequest="40" 
                                    CornerRadius="12"
                                    BorderWidth="0"
                                    Margin="0,5,0,0"
                                    Command="{Binding DownloadWeeklyReportCommand}"/>
                        </Grid>
                    </Frame>

                    <!-- Monthly Picks -->
                    <Frame BackgroundColor="White" CornerRadius="12" Padding="12" HasShadow="True" Grid.Column="1">
                        <Grid RowDefinitions="Auto,Auto,Auto,Auto" ColumnDefinitions="*,Auto">
                            <Grid ColumnDefinitions="*,Auto" Grid.Row="0">
                                <Label Text="Monthly Picks" FontAttributes="Bold" FontSize="18" TextColor="#5B4F45"/>
                                <Label Text="{Binding MonthlyOrders, StringFormat='{0} Orders'}" Grid.Column="1" FontSize="14" TextColor="Gray"/>
                            </Grid>
                            <!-- Bar chart for Monthly items -->
                            <CollectionView Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding TopItemsMonthly}" HeightRequest="180" ItemsLayout="HorizontalList">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <VerticalStackLayout HorizontalOptions="Center" WidthRequest="90" Spacing="6" Padding="0">
                                            <Grid HeightRequest="150" BackgroundColor="Transparent" VerticalOptions="FillAndExpand">
                                                <Border VerticalOptions="End" 
                                                        HeightRequest="{Binding ScaledCount}" 
                                                        WidthRequest="50" 
                                                        Background="{Binding ColorCode, Converter={StaticResource StringToColorConverter}}" 
                                                        StrokeThickness="0"
                                                        Stroke="Transparent">
                                                    <Border.StrokeShape>
                                                        <RoundRectangle CornerRadius="12"/>
                                                    </Border.StrokeShape>
                                                </Border>
                                            </Grid>
                                            <Label Text="{Binding Name}" TextColor="#8B7355" FontSize="11"  LineBreakMode="TailTruncation" HorizontalTextAlignment="Center"/>
                                        </VerticalStackLayout>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                            <CollectionView ItemsSource="{Binding TopItemsMonthly}" HeightRequest="60" Grid.Row="2" Grid.ColumnSpan="2" Margin="0,0,0,0">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnDefinitions="*,Auto">
                                            <Label Text="{Binding Name}" FontSize="12" TextColor="Gray"/>
                                            <Label Text="{Binding Count, StringFormat='{0} sold'}" FontSize="12" TextColor="Gray" Grid.Column="1"/>
                                        </Grid>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                            <!-- PDF Download Button at bottom -->
                            <Button Grid.Row="3" 
                                    Grid.ColumnSpan="2"
                                    Text="📄 Download Monthly Report" 
                                    FontSize="13" 
                                    FontAttributes="Bold"
                                    TextColor="#5B4F45" 
                                    BackgroundColor="#FFB3D9" 
                                    HeightRequest="40" 
                                    CornerRadius="12"
                                    BorderWidth="0"
                                    Margin="0,5,0,0"
                                    Command="{Binding DownloadMonthlyReportCommand}"/>
                        </Grid>
                    </Frame>
                </Grid>

<<<<<<< Updated upstream
                
=======
                <!-- Sales Breakdown Table -->
                <Frame BackgroundColor="White" CornerRadius="12" Padding="16" HasShadow="True">
                    <VerticalStackLayout Spacing="12">
                        <Grid ColumnDefinitions="*,Auto">
                            <Label Text="Sales Breakdown" FontAttributes="Bold" FontSize="18" TextColor="#5B4F45"/>
                            <Button Grid.Column="1"
                                    Text="{Binding AggregateToggleText}"
                                    Command="{Binding ToggleAggregateSectionCommand}"
                                    FontSize="12"
                                    FontAttributes="Bold"
                                    TextColor="White"
                                    BackgroundColor="#8B7355"
                                    CornerRadius="10"
                                    Padding="14,6"/>
                        </Grid>

                        <Grid ColumnDefinitions="Auto,*" IsVisible="{Binding IsAggregateSectionVisible}">
                            <Label Text="Group by:" FontSize="12" TextColor="#5B4F45" VerticalOptions="Center"/>
                            <Picker Grid.Column="1"
                                    ItemsSource="{Binding AggregateModes}"
                                    SelectedItem="{Binding SelectedAggregateMode}"
                                    FontSize="12"
                                    TextColor="#5B4F45"
                                    HorizontalOptions="End"
                                    WidthRequest="200"/>
                        </Grid>

                        <ActivityIndicator IsVisible="{Binding IsAggregateLoading}" IsRunning="{Binding IsAggregateLoading}" Color="#2E7D32"/>

                        <StackLayout IsVisible="{Binding IsAggregateSectionVisible}">
                            <StackLayout IsVisible="{Binding HasAggregateRows}">
                                <ScrollView Orientation="Horizontal">
                                    <VerticalStackLayout Spacing="8">
                                        <Border Stroke="#D5C3AB" StrokeThickness="1" StrokeShape="RoundRectangle 6" Padding="0">
                                            <Grid ColumnDefinitions="180,80,120,120,120,120" Padding="0,0,8,0" RowDefinitions="Auto">
                                                <Label Text="Group" FontAttributes="Bold" TextColor="#5B4F45" Padding="12,8"/>
                                                <BoxView Grid.Column="1" Color="#E0E0E0" WidthRequest="1" HorizontalOptions="End" Margin="0" InputTransparent="True"/>
                                                <Label Grid.Column="1" Text="Total Qty" FontAttributes="Bold" TextColor="#5B4F45" HorizontalTextAlignment="Center" Padding="12,8"/>
                                                <BoxView Grid.Column="2" Color="#E0E0E0" WidthRequest="1" HorizontalOptions="End" Margin="0" InputTransparent="True"/>
                                                <Label Grid.Column="2" Text="Total Revenue" FontAttributes="Bold" TextColor="#5B4F45" HorizontalTextAlignment="Center" Padding="12,8"/>
                                                <BoxView Grid.Column="3" Color="#E0E0E0" WidthRequest="1" HorizontalOptions="End" Margin="0" InputTransparent="True"/>
                                                <Label Grid.Column="3" Text="Cash (Qty / ₱)" FontAttributes="Bold" TextColor="#5B4F45" HorizontalTextAlignment="Center" Padding="12,8"/>
                                                <BoxView Grid.Column="4" Color="#E0E0E0" WidthRequest="1" HorizontalOptions="End" Margin="0" InputTransparent="True"/>
                                                <Label Grid.Column="4" Text="GCash (Qty / ₱)" FontAttributes="Bold" TextColor="#5B4F45" HorizontalTextAlignment="Center" Padding="12,8"/>
                                                <BoxView Grid.Column="5" Color="#E0E0E0" WidthRequest="1" HorizontalOptions="End" Margin="0" InputTransparent="True"/>
                                                <Label Grid.Column="5" Text="Bank (Qty / ₱)" FontAttributes="Bold" TextColor="#5B4F45" HorizontalTextAlignment="Center" Padding="12,8"/>
                                            </Grid>
                                        </Border>

                                        <CollectionView ItemsSource="{Binding SalesAggregateRows}" HeightRequest="260" SelectionMode="None">
                                            <CollectionView.ItemsLayout>
                                                <LinearItemsLayout Orientation="Vertical" ItemSpacing="4"/>
                                            </CollectionView.ItemsLayout>
                                            <CollectionView.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Stroke="#E0D4C3" StrokeThickness="1" StrokeShape="RoundRectangle 4" Padding="0">
                                                        <Grid ColumnDefinitions="180,80,120,120,120,120" ColumnSpacing="0" Padding="0,6,8,6">
                                                            <Label Text="{Binding GroupLabel}" TextColor="#424242" FontSize="13" LineBreakMode="WordWrap" Padding="12,0"/>
                                                            <BoxView Grid.Column="1" Color="#EFE2D3" WidthRequest="1" HorizontalOptions="End" InputTransparent="True" Margin="0"/>
                                                            <Label Grid.Column="1" Text="{Binding TotalQuantityDisplay}" TextColor="#424242" FontSize="13" HorizontalTextAlignment="Center" Padding="12,0"/>
                                                            <BoxView Grid.Column="2" Color="#EFE2D3" WidthRequest="1" HorizontalOptions="End" InputTransparent="True" Margin="0"/>
                                                            <Label Grid.Column="2" Text="{Binding TotalRevenueDisplay}" TextColor="#424242" FontSize="13" HorizontalTextAlignment="Center" Padding="12,0"/>
                                                            <BoxView Grid.Column="3" Color="#EFE2D3" WidthRequest="1" HorizontalOptions="End" InputTransparent="True" Margin="0"/>
                                                            <VerticalStackLayout Grid.Column="3" Spacing="2" HorizontalOptions="Center" Padding="12,0">
                                                                <Label Text="{Binding Cash.QuantityDisplay}" TextColor="#424242" FontSize="12" HorizontalTextAlignment="Center"/>
                                                                <Label Text="{Binding Cash.RevenueDisplay}" TextColor="#2E7D32" FontSize="12" HorizontalTextAlignment="Center"/>
                                                            </VerticalStackLayout>
                                                            <BoxView Grid.Column="4" Color="#EFE2D3" WidthRequest="1" HorizontalOptions="End" InputTransparent="True" Margin="0"/>
                                                            <VerticalStackLayout Grid.Column="4" Spacing="2" HorizontalOptions="Center" Padding="12,0">
                                                                <Label Text="{Binding GCash.QuantityDisplay}" TextColor="#424242" FontSize="12" HorizontalTextAlignment="Center"/>
                                                                <Label Text="{Binding GCash.RevenueDisplay}" TextColor="#1976D2" FontSize="12" HorizontalTextAlignment="Center"/>
                                                            </VerticalStackLayout>
                                                            <BoxView Grid.Column="5" Color="#EFE2D3" WidthRequest="1" HorizontalOptions="End" InputTransparent="True" Margin="0"/>
                                                            <VerticalStackLayout Grid.Column="5" Spacing="2" HorizontalOptions="Center" Padding="12,0">
                                                                <Label Text="{Binding Bank.QuantityDisplay}" TextColor="#424242" FontSize="12" HorizontalTextAlignment="Center"/>
                                                                <Label Text="{Binding Bank.RevenueDisplay}" TextColor="#7B1FA2" FontSize="12" HorizontalTextAlignment="Center"/>
                                                            </VerticalStackLayout>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </CollectionView.ItemTemplate>
                                        </CollectionView>
                                    </VerticalStackLayout>
                                </ScrollView>
                                <Label Text="Figures include all filtered transactions." FontSize="11" TextColor="#757575" Margin="0,4,0,0"/>
                            </StackLayout>
                            <Label Text="No sales data for this range yet." FontSize="13" TextColor="#757575" HorizontalTextAlignment="Center" Margin="0,12,0,0" IsVisible="{Binding HasAggregateRows, Converter={StaticResource InvertBoolConverter}}"/>
                        </StackLayout>
                    </VerticalStackLayout>
                </Frame>

>>>>>>> Stashed changes
            </VerticalStackLayout>
        </ScrollView>

        <!-- All Global Popups -->
        <views:Settings BindingContext="{Binding SettingsPopup}" Grid.ColumnSpan="2"/>
        <otherViews:ManagePOSOptions BindingContext="{Binding Source={x:Static Application.Current}, Path=ManagePOSPopup}" Grid.ColumnSpan="2" ZIndex="1800"/>
        <otherViews:EditProductPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=ManagePOSPopup.EditProductPopupVM}" Grid.ColumnSpan="2" ZIndex="1900"/>
        <otherViews:ManageInventoryOptions BindingContext="{Binding Source={x:Static Application.Current}, Path=ManageInventoryPopup}" Grid.ColumnSpan="2" ZIndex="1800"/>
        <otherViews:EditInventoryPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=EditInventoryPopup}" Grid.ColumnSpan="2"/>
        <otherViews:AddItemToInventory BindingContext="{Binding Source={x:Static Application.Current}, Path=ManageInventoryPopup.AddItemToInventoryVM}" Grid.ColumnSpan="2"/>
        <otherViews:UpdateInventoryDetails BindingContext="{Binding Source={x:Static Application.Current}, Path=ManageInventoryPopup.AddItemToInventoryVM}" Grid.ColumnSpan="2" ZIndex="1400"/>
        <otherViews:AddItemToPOS BindingContext="{Binding Source={x:Static Application.Current}, Path=AddItemPopup}" Grid.ColumnSpan="2"/>
        <otherViews:ConnectPOSItemToInventory BindingContext="{Binding Source={x:Static Application.Current}, Path=AddItemPopup.ConnectPOSToInventoryVM}" Grid.ColumnSpan="2" ZIndex="1100"/>
        <otherViews:InputIngredientsAmountUsed BindingContext="{Binding Source={x:Static Application.Current}, Path=AddItemPopup.ConnectPOSToInventoryVM}" Grid.ColumnSpan="2" ZIndex="1200"/>
        <otherViews:PreviewPOSItem BindingContext="{Binding Source={x:Static Application.Current}, Path=AddItemPopup.ConnectPOSToInventoryVM}" Grid.ColumnSpan="2" ZIndex="1300"/>
        <otherViews:AddonsSelectionPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=AddItemPopup.ConnectPOSToInventoryVM.AddonsPopup}" Grid.ColumnSpan="2" ZIndex="1400"/>
        <otherViews:CartPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=POSVM.CartPopup}" Grid.ColumnSpan="2"/>
        <otherViews:HistoryPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=HistoryPopup}" Grid.ColumnSpan="2"/>
        <otherViews:PaymentPopup x:Name="PaymentPopupControl" Grid.ColumnSpan="2"/>
        <!-- Removed OrderCompletePopup here; it should only appear on POS -->
        <otherViews:OrderConfirmedPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=OrderConfirmedPopup}" Grid.ColumnSpan="2" ZIndex="2000"/>
        <otherViews:SuccessCardPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=SuccessCardPopup}" Grid.ColumnSpan="2" ZIndex="1200"/>
        <otherViews:NotificationPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=NotificationPopup}" Grid.ColumnSpan="2" ZIndex="1500"/>
        <otherViews:ProfilePopup BindingContext="{Binding Source={x:Static Application.Current}, Path=ProfilePopup}" Grid.ColumnSpan="2" ZIndex="1600"/>
        <otherViews:PasswordResetPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=PasswordResetPopup}" Grid.ColumnSpan="2" ZIndex="1700"/>
    </Grid>
</ContentPage>
