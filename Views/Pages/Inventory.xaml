<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Coftea_Capstone.Views.Pages.Inventory"
             xmlns:view="clr-namespace:Coftea_Capstone.Views.Pages"
             xmlns:otherViews="clr-namespace:Coftea_Capstone.Views.Controls"
             xmlns:converters="clr-namespace:Coftea_Capstone.ViewModel.Others"
             Title="Inventory"
             BackgroundColor="#D6BCA8">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:InvertBoolConverter x:Key="InvertBoolConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid ColumnDefinitions="200, *" RowDefinitions="*">
        <!-- Sidebar Navigation -->
        <view:NavigationBar Grid.Column="0"/>

        <!-- Main Content -->
        <Grid Grid.Column="1" Padding="20" RowDefinitions="Auto,*">

            <VerticalStackLayout Padding="0" Spacing="20">

                <!-- Top Bar -->
                <Grid ColumnDefinitions="*, Auto, Auto, Auto" VerticalOptions="Center" ColumnSpacing="25">
                    <Label Text="Inventory" FontSize="32" FontAttributes="Bold" VerticalOptions="Center" TextColor="Black"/>
                    <ImageButton Grid.Column="2" Source="notification.png" WidthRequest="35" HeightRequest="35" BackgroundColor="Transparent" Clicked="OnBellClicked"/>
                    <ImageButton Source="usericon.png" Grid.Column="3" WidthRequest="40" HeightRequest="40"/>
                </Grid>

                <!-- Brownish Frame (Main Panel) -->
                <Frame BackgroundColor="#7E6D5F" CornerRadius="15" Padding="20" HasShadow="False" HeightRequest="600">

                    <VerticalStackLayout Spacing="15">

                        <!-- Fixed Top Controls (Category Buttons, Sort By) -->
                        <Grid ColumnDefinitions="*, Auto" VerticalOptions="Start">
                            <!-- Left: Category Buttons -->
                            <HorizontalStackLayout Spacing="15" VerticalOptions="Center" HorizontalOptions="Start">
                                <Button Text="Ingredients" 
                                        TextColor="Black" 
                                        FontAttributes="Bold" 
                                        BackgroundColor="#FFEAD6" 
                                        WidthRequest="120" 
                                        HeightRequest="40" 
                                        CornerRadius="7" 
                                        Command="{Binding FilterByCategoryCommand}" 
                                        CommandParameter="Ingredients">
                                    <Button.Triggers>
                                        <DataTrigger TargetType="Button" Binding="{Binding SelectedCategory}" Value="Ingredients">
                                            <Setter Property="BackgroundColor" Value="#EBCFBF" />
                                            <Setter Property="TextColor" Value="#2b2b2b" />
                                        </DataTrigger>
                                    </Button.Triggers>
                                </Button>
                                <Button Text="Supplies" 
                                        TextColor="Black" 
                                        FontAttributes="Bold" 
                                        BackgroundColor="#FFEAD6" 
                                        WidthRequest="120" 
                                        HeightRequest="40" 
                                        CornerRadius="7" 
                                        Command="{Binding FilterByCategoryCommand}" 
                                        CommandParameter="Supplies">
                                    <Button.Triggers>
                                        <DataTrigger TargetType="Button" Binding="{Binding SelectedCategory}" Value="Supplies">
                                            <Setter Property="BackgroundColor" Value="#FFE0C4" />
                                            <Setter Property="TextColor" Value="#2b2b2b" />
                                        </DataTrigger>
                                    </Button.Triggers>
                                </Button>
                                <!-- Search by name -->
                                <Grid ColumnDefinitions="Auto, *" VerticalOptions="Center" Padding="10,0,0,0">
                                    <Label Text="Search:" VerticalOptions="Center" TextColor="White"/>
                                    <Entry Grid.Column="1"
                                           WidthRequest="220"
                                           BackgroundColor="Transparent"
                                           TextColor="Black"
                                           PlaceholderColor="White"
                                           ClearButtonVisibility="WhileEditing"
                                           Placeholder="Type item name..."
                                           Text="{Binding SearchText}"/>
                                </Grid>
                            </HorizontalStackLayout>

                            <!-- Right: Sort By -->
                            <Frame Grid.Column="1"
                                   CornerRadius="10" 
                                   BackgroundColor="White" 
                                   Padding="0" 
                                   WidthRequest="200" 
                                   HeightRequest="40"
                                   HorizontalOptions="End">
                                <Picker Title="Sort By"
                                        VerticalOptions="Center"
                                        Margin="0"                               
                                        BackgroundColor="Transparent"
                                        TextColor="Black"
                                        TitleColor="#666666"
                                        FontSize="14">
                                    <Picker.Items>
                                        <x:String>Name (A-Z)</x:String>
                                        <x:String>Name (Z-A)</x:String>
                                        <x:String>Price (Low to High)</x:String>
                                        <x:String>Price (High to Low)</x:String>
                                    </Picker.Items>
                                </Picker>
                            </Frame>
                        </Grid>

                        <!-- Inventory Content with fixed header and scrollable items -->
                        <Grid RowDefinitions="Auto,*">

                            <!-- Loading Indicator -->
                            <VerticalStackLayout Grid.Row="0" HorizontalOptions="Center" VerticalOptions="Start" IsVisible="{Binding IsLoading}">
                                <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" />
                                <Label Text="{Binding StatusMessage}" TextColor="Black" FontSize="14" HorizontalOptions="Center" />
                            </VerticalStackLayout>

                            <!-- Fixed Header Row -->
                            <Frame Grid.Row="0" BackgroundColor="White" Padding="10" IsVisible="{Binding IsLoading, Converter={StaticResource InvertBoolConverter}}">
                                <Grid ColumnDefinitions="2*, 1*, 2*">
                                    <Label Text="ITEMS" FontSize="15" FontAttributes="Bold" VerticalOptions="Center" HorizontalOptions="Start" TextColor="Black"/>
                                    <Label Text="CATEGORY" FontSize="15" FontAttributes="Bold" VerticalOptions="Center" HorizontalOptions="Center" Grid.Column="1" TextColor="Black"/>
                                    <Label Text="STOCKS" FontSize="15" FontAttributes="Bold" VerticalOptions="Center" HorizontalOptions="Center" Grid.Column="2" TextColor="Black"/>
                                </Grid>
                            </Frame>

                            <!-- Inventory CollectionView: only this scrolls -->
                            <CollectionView Grid.Row="1" x:Name="InventoryCollectionView" 
                                            ItemsSource="{Binding InventoryItems}"
                                            ItemsLayout="VerticalList" 
                                            BackgroundColor="Transparent" 
                                            Margin="0,10,0,0"
                                            IsVisible="{Binding IsLoading, Converter={StaticResource InvertBoolConverter}}">

                                <CollectionView.ItemTemplate>
                                        <DataTemplate>
                                            <Frame Padding="10" Margin="0,6,0,6" CornerRadius="10" BackgroundColor="White">
                                                <VerticalStackLayout Spacing="6">
                                                    <Grid ColumnDefinitions="2*, 1*, 2*" VerticalOptions="Center" ColumnSpacing="10">
                                                        <!-- Items: image + name -->
                                                        <HorizontalStackLayout Grid.Column="0" Spacing="10" VerticalOptions="Center">
                                                            <Frame WidthRequest="50" HeightRequest="50" CornerRadius="6" Padding="0" HasShadow="False" BackgroundColor="#F0F0F0">
                                                                <Image Source="{Binding ImageSource}" Aspect="AspectFill"/>
                                                            </Frame>
                                                            <Label Text="{Binding itemName}" FontSize="16" VerticalOptions="Center" TextColor="Black"/>
                                                        </HorizontalStackLayout>

                                                        <!-- Category -->
                                                        <Label Text="{Binding itemCategory}" Grid.Column="1" VerticalOptions="Center" HorizontalOptions="Center" TextColor="Black"/>

                                                        <!-- Stock Progress -->
                                                        <VerticalStackLayout Grid.Column="2" Spacing="6" VerticalOptions="Center">
                                                            <Frame BackgroundColor="White" CornerRadius="10" Padding="6" HasShadow="True">
                                                                <Grid RowDefinitions="Auto,Auto" VerticalOptions="Center">
                                                                    <Grid HeightRequest="22">
                                                                        <BoxView Color="#E0E0E0" CornerRadius="17" />
                                                                        <ProgressBar Progress="{Binding StockProgress}"
                                                                                     ProgressColor="{Binding StockFillColor}"
                                                                                     HeightRequest="34" />
                                                                    </Grid>
                                                                    <Label Grid.Row="1" FontSize="12" TextColor="Black" HorizontalOptions="Center" VerticalOptions="Center">
                                                                        <Label.Text>
                                                                            <MultiBinding StringFormat="{}{0} {1}">
                                                                                <Binding Path="StockDisplay"/>
                                                                                <Binding Path="MinimumDisplay"/>
                                                                            </MultiBinding>
                                                                        </Label.Text>
                                                                    </Label>
                                                                </Grid>
                                                            </Frame>
                                                        </VerticalStackLayout>
                                                    </Grid>
                                                </VerticalStackLayout>
                                            </Frame>
                                        </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </Grid>
                    </VerticalStackLayout>
                </Frame>
            </VerticalStackLayout>
        </Grid>

        <!-- Popups -->
        <view:Settings BindingContext="{Binding SettingsPopup}" Grid.ColumnSpan="2"/>
        <otherViews:ManagePOSOptions BindingContext="{Binding Source={x:Static Application.Current}, Path=ManagePOSPopup}" Grid.ColumnSpan="2"/>
        <otherViews:EditProductPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=ManagePOSPopup.EditProductPopupVM}" Grid.ColumnSpan="2"/>
        <otherViews:ManageInventoryOptions BindingContext="{Binding Source={x:Static Application.Current}, Path=ManageInventoryPopup}" Grid.ColumnSpan="2"/>
        <otherViews:AddItemToInventory BindingContext="{Binding Source={x:Static Application.Current}, Path=ManageInventoryPopup.AddItemToInventoryVM}" Grid.ColumnSpan="2"/>
        <otherViews:EditInventoryPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=EditInventoryPopup}" Grid.ColumnSpan="2"/>
        <otherViews:UpdateInventoryDetails BindingContext="{Binding Source={x:Static Application.Current}, Path=ManageInventoryPopup.AddItemToInventoryVM}" Grid.ColumnSpan="2" ZIndex="1400"/>
        <otherViews:AddItemToPOS BindingContext="{Binding Source={x:Static Application.Current}, Path=AddItemPopup}" Grid.ColumnSpan="2"/>
        <otherViews:RetryConnectionPopup x:Name="RetryConnectionPopup" Grid.ColumnSpan="2"/>
        <otherViews:SuccessCardPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=SuccessCardPopup}" Grid.ColumnSpan="2" ZIndex="1200"/>
        <otherViews:NotificationPopup BindingContext="{Binding Source={x:Static Application.Current}, Path=NotificationPopup}" Grid.ColumnSpan="2" ZIndex="1500"/>
    </Grid>
</ContentPage>
